{
  "name": "@wynding/content",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "AGPL-3.0-or-later AND CC-BY-SA-4.0",
  "description": "Level and wave data. Code (types/loaders) is AGPL-3.0-or-later; authored content data is CC-BY-SA-4.0 (see README).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "lint": "eslint src",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@wynding/types": "workspace:*"
  }
}
