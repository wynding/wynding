{
  "name": "@wynding/platform",
  "version": "0.0.0",
  "description": "The shared platform seam ADR 0008 names: the async StorageDriver, the versioned save envelope, and the serialized single-writer save slot. Imported by apps only — never by the sim.",
  "private": true,
  "type": "module",
  "license": "AGPL-3.0-or-later",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b && tsc --noEmit -p tsconfig.tests.json",
    "lint": "eslint src",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist *.tsbuildinfo"
  }
}
