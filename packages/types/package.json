{
  "name": "@wynding/types",
  "version": "0.0.0",
  "description": "Shared types: the ADR 0007 ruleset-schema vocabulary and the domain types with production consumers.",
  "private": true,
  "type": "module",
  "license": "AGPL-3.0-or-later",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b && tsc --noEmit -p tsconfig.tests.json",
    "lint": "eslint src",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist *.tsbuildinfo"
  }
}
