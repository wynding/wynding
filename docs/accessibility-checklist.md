# Accessibility checklist (ADR 0003 §3)

ADR 0003 requires this checklist to land **with the first real UI** (M1 Story 6 — Render,
input & HUD). Each GAG §2 day-one item is either **implemented** in Story 6 or carries an
**explicit, justified waiver**. axe-core runs in CI via the Playwright e2e suite against the
DOM UI and fails the build on any violation; the Phaser canvas (which axe cannot inspect) is
covered by the manual items below. This checklist is re-audited in full at Story 7 (the M1
conformance + contrast sign-off) — Story 6 builds the enforcement mechanisms and ticks what it
introduces. It is re-audited again at Story 10 (Playable UX: app-shell layout, touch-first
placement, player-started runs), which replaces the Story 6-era two-tap touch gesture with
press-adjust-release, introduces the Shell/Rail/Card/Panel/Dock layout and its single modal
owner (results/rotate/settings), and adds the portrait rotate prompt — see the Story 10
conformance audit below (M1-S10).

Legend: ✅ implemented · 🟡 partial / session-scoped · ⛔ waived (with reason).

## GAG §2 — day-one items

| Item                                                 | Status | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ---------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Colourblind-safe palette + dual encoding             | ✅     | Okabe–Ito / Paul Tol hues; every role also has a distinct shape (creep = triangle, tower = rounded square, valid ghost = solid outline, invalid = crossed box). Colour is never the sole signal. `packages/render/src/palette.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Selectable colourblind modes (protan/deutan/tritan)  | ✅     | Settings radio group re-maps the palette off the relevant confusion axis. Session-scoped.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Reduced-motion setting                               | ✅     | Damps (shortens + fades) the impact-spark FX; `prefers-reduced-motion` is honoured at boot. CSS also disables transitions/animations under the media query.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Full functionality across touch / mouse / keyboard   | ✅     | Mouse = hover-preview + click-commit; touch/pen = **press-adjust-release** on the **Armed** state (press shows an offset ghost 2 cells above the finger, moving adjusts it, release commits — two-tap is gone, Story 10 deleted it); Card gestures disambiguate tap (arm/disarm) from drag-from-rail (arm + press-adjust-release) by an 8px threshold; keyboard = focusable board cursor (arrows + confirm/sell/start) plus the `armTower1` hotkey (arms from any state) and focusable HUD/Rail/Dock controls. `apps/web/src/input.ts`, `apps/web/src/controller.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Visible focus indicators                             | ✅     | 3px focus ring on the board and every control (`:focus-visible`, `ui.css`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ≥ 44 × 44 CSS-px touch targets                       | ✅     | All `.wy-btn` controls (Dock, Card, Panel actions, settings) have `min-width`/`min-height: 44px`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Remappable / rebindable controls                     | ✅ 🟡  | Every game action — including Story 10's `armTower1` (Card hotkey) and the renamed `start` action — is rebindable via the settings dialog (`keymap.ts`), with a live hotkey badge on the Card. Session-scoped — cross-session persistence is deferred to Phase 2 per ADR 0008.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| DOM/HUD text resize to 200% without loss of function | ✅     | HUD/controls are semantic DOM (not canvas text). The page itself never scrolls, but `.wy-hud` (the status chips list), `.wy-rail` (Rail), the Compact column Dock, and the settings dialog each get `overflow-y: auto`, so 200% zoom reflows and scrolls those regions internally instead of clipping — the board viewport is the only fixed-fit element. Story 11 moved the bounded scrollport from `.wy-status` onto `.wy-hud` (the Dock is now a status child, and an absolutely-positioned Standard Dock would be clipped by an ancestor scroll box) and made `.wy-hud` a keyboard-operable scrollable region with its own tab stop in BOTH layouts. `apps/web/e2e/smoke.spec.ts` pins this at the smallest supported landscape viewport (658×320, the Galaxy S9+ landscape profile — Compact after Story 11) and proves reachability per region: focus + arrow-key `scrollTop` change on the chips scrollport, focus + scrollport-visibility for the Rail's, Dock's, and settings' last controls.                                      |
| Pause                                                | ✅     | Full calm-planning pause (build/sell allowed while paused). Story 10 adds player-started runs: a fresh run/Play-again holds at tick 0 (`started = false`) until the player presses **Start** — no hidden countdown — and Space/speed changes never un-hold; the Dock's Pause button is hidden (not disabled) pre-start, since there is nothing yet to pause.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Contrast ≥ 4.5:1                                     | ✅     | Enforced by `packages/render/src/palette.test.ts` (canvas cues ≥ 3:1 vs floor across all four colour modes, measured min 3.17) and `apps/web/src/ui-contrast.test.ts` (DOM text ≥ 4.5:1, measured min 7.17; non-text ≥ 3:1, measured min 5.97), both inside `pnpm run verify`. Sign-off recorded in the Story 7 audit below; Story 10 extends the rendered-contrast e2e spot checks to the Card, Panel, Dock, and the rotate overlay.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| No flashing (WCAG 2.3.1)                             | ✅     | No strobing FX; the impact-spark is a single brief fade, well under 3 flashes/sec.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Semantic controls / labels for AT                    | ✅     | Real `<button>`s; the HUD is a labelled `role="group"` BY DESIGN (a polite live region would flood AT during combat — `overlay.ts:97-102`); a dedicated polite `aria-live` region (Story 10) announces armed/disarmed, placement success/rejection, sell + refund, and (M2-S9) tower DESTRUCTION — the one announcement the player did not initiate; results, settings, and the Story 10 rotate prompt are each a labelled `role="dialog"` behind one modal owner (`modal.ts`) that owns `inert` + focus save/restore across a `results > rotate > settings` priority stack; the board is a labelled `role="application"`. The Panel's "Max level" Upgrade control is a focusable `aria-disabled` element (not a native `disabled` button), so it stays discoverable/reachable by keyboard and AT while its activation is suppressed. The axe audit covers the settings, results, armed-Panel, and selected-Panel states (desktop and, via `chromium-touch`, under touch), plus the inert/Tab-containment/focus-restore modal checks (e2e). |
| All user-facing text is translatable                 | ✅     | Every string resolves through the typed `t()` catalog (ADR 0004); the `no-ui-literals` lint rule bans raw literals in text sinks.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |

## Waivers

| Item                       | Status | Reason                                                                                                                                                                                 |
| -------------------------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Selectable difficulty (§3) | ⛔     | M1 ships **Medium-only** — a pre-existing PRD/ADR-sanctioned M1 waiver, **finalized in Story 7** (also recorded in the Story 7 PR description per ADR 0003 §3). Not a §2 day-one item. |

No §2 day-one items are waived (Rob ratified full §2 compliance in Story 6).

## Canvas-covered items (not visible to axe — verified manually / by unit tests)

- Dual-encoding of every board entity (shape + colour) — unit-tested palette distinctness;
  visual check of creep/tower/ghost silhouettes.
- Reduced-motion damping of the impact-spark — visual check with the setting on/off.
- Keyboard board cursor movement + build/select/sell — driven by `input.ts` unit tests and
  the e2e smoke.
- The armed offset ghost's on-canvas position (2 cells above the finger, flipping below near
  the top edge) — unit-tested in `input.ts`; `apps/web/e2e/touch.spec.ts` additionally proves
  the RENDERED commit lands at the offset anchor (a real screenshot sample), not just the
  DOM-side intent.

## Enforcement mechanisms landed in Story 6

- **axe-core in CI** via the Playwright e2e (`apps/web/e2e/smoke.spec.ts`) — fails on any
  violation against the DOM UI.
- **i18n**: typed `t()` catalog, the `no-ui-literals` ESLint rule, and the extraction +
  cross-locale check (`scripts/i18n-check.mjs`), all wired into `pnpm run verify`.

## Deferred to Story 7 (delivered)

- Final ADR 0003 conformance audit + contrast sign-off across the whole M1 slice — done;
  see the Story 7 conformance audit section below.

Deferred to Phase 2 (ADR 0008): cross-session persistence of accessibility/control settings
(colour mode, reduced motion, rebinds). Alpha players re-apply their preferences per visit —
accepted for M1; ADR 0008's `StorageDriver` seam lands the persistence in Phase 2.

Deferred to #36 (the real-device playtest that naturally follows Story 10): every item this
checklist can only assess synthetically. The Playwright suite (including `chromium-touch`'s
CDP-driven touch/rotate coverage) proves the LOGIC of press-adjust-release, the tap/drag
threshold, and the rotate prompt is correct; it cannot prove the smallest-phone board is
comfortable to actually build on with a real finger, nor that synthetic touch events fully
match real touchscreen fidelity (PLAN.md's "Risks / open questions"). Both are explicitly
carried forward to #36, not claimed as done here.

## Story 7 conformance audit (M1-S7, 2026-07-23)

Full re-audit against this checklist, landing the permanent contrast gate and finalizing the
§3 waiver. Figures below are **as-measured on the audit date, re-derivable by running
`packages/render/src/palette.test.ts`** (which always prints the per-colour-mode minima on every
run) — dated evidence backed by a reproducible source, not a hand-maintained number.

**Canvas cues vs the board floor (WCAG 1.4.11 non-text, ≥ 3:1), per colour mode:**

| Colour mode | Measured minimum | Binding cue(s)                      |
| ----------- | ---------------- | ----------------------------------- |
| default     | 3.32             | `range` composited @ 0.7 alpha      |
| protan      | 3.17             | `tower` / `ghostValid` (`0x0072b2`) |
| deutan      | 3.17             | `tower` / `ghostValid` (`0x0072b2`) |
| tritan      | 4.18             | `range` composited @ 0.7 alpha      |

**DOM tokens (`ui.css`):** text pairs (WCAG ≥ 4.5:1) measured minimum **7.17** (`#04121f` on
`--wy-accent`); non-text pairs (≥ 3:1) measured minimum **5.97** (`--wy-accent` on
`--wy-surface`).

**Exemptions:**

- `spark` — exempt from the gate: transient fading FX (alpha → 0 by design), non-essential
  (the kill outcome is carried by the creep/HP-pip state), and reduced-motion governed.
- `border` (1.66:1) — excluded: a deliberate quiet structural fill whose identity is carried
  by geometry (the outer ring), not colour; the openings (entrance/exit) it borders carry
  ≥ 4.26:1 glyphs.

**Re-audited rows — status confirmed:**

- Colourblind-safe palette + dual encoding — ✅, now backed by a permanent automated gate
  (`palette.test.ts`) in addition to the prior unit-tested distinctness check.
- Contrast ≥ 4.5:1 — ✅, gated at the correct WCAG bar (3:1 non-text / 4.5:1 text) rather
  than the palette header's prior aspirational (and incorrect) "≥ 4.5:1 everywhere" claim;
  `palette.ts`'s header comment is corrected to match.
- Remappable / rebindable controls — ✅ 🟡, persistence line corrected to cite Phase 2 (ADR
  0008), not "deferred to Story 7" — that line was in error.
- Semantic controls / labels for AT — ✅, the false "HUD is a polite live region" claim is
  corrected (it is `role="group"` by design); the axe audit now also covers the results
  dialog and the inert/Tab-containment/focus-restore modal behaviour (e2e).
- Selectable difficulty (§3 waiver) — ⛔, finalized this story; recorded in the PR
  description per ADR 0003 §3.
- i18n copy conformance — the `hud.bounty` copy violation (#33, `"Gold:"` → `"Bounty:"`) and the
  non-canonical "wynd" loss line (→ "The creeps broke through.") are both corrected.
- Input edge cases (#34) — key-repeat no longer oscillates discrete actions (pause/sell/
  call-wave/speed edge-triggered; movement still auto-repeats), and the touch two-tap gesture
  now uses a uniform confirm/arm/clear rule with per-pointer press-origin tracking and a
  concurrent-multi-touch void, closing the tower-select-then-retap instant-build bug and the
  cross-finger press/release defeat.

## Story 10 conformance audit (M1-S10, 2026-07-24)

Full re-audit against this checklist for Story 10 (Playable UX: app-shell layout,
touch-first placement, player-started runs). Story 10 is a layout/interaction story with
**zero sim/engine/content changes** (SIM_VERSION stays 5) — this audit covers the new DOM
surface and interaction model it adds on top of the Story 7 sign-off above, which still
holds for the canvas cues and DOM contrast tokens.

**Two-tap is gone — replaced by press-adjust-release + the Armed model.** The Story 6/7 rows
above described touch as "two-tap preview-then-confirm"; that gesture no longer exists.
Touch/pen now presses to show an offset ghost (2 cells above the finger, flipping below near
the top edge so the finger never covers its own footprint), adjusts on move, and commits on
release — one gesture, not two taps on the same cell. **Armed** (defined in PLAN.md's
vocabulary section, extending docs/CONTEXT.md's glossary) is the purely-`apps/web` UI state
"a tower kind is chosen for placement" — it never enters the sim or the replay log — and
drives one normative event table (PLAN.md P2) shared by mouse, keyboard, and touch/pen: arm
via Card click/tap/hotkey or `armTower1`; armed placement never re-arms; an
occupied/unaffordable/blocked cell rejects with a persistent invalid ghost (no flash timer)
and stays armed; `Escape`/Close disarm or deselect one layer at a time.

**New layout states recorded:**

- **Shell** (`.wy-shell`, the viewport-filling grid) / **Status bar** (`.wy-status`) /
  **Rail** (`.wy-rail`, one **Card** at M1) / **Panel** (`.wy-panel`, opens inside the Rail
  below the Card) / **Dock** (`.wy-dock`, bottom-left, overlapping the board border) — the
  page itself never scrolls (`body { overflow: hidden }`), but the Status bar, Rail, and
  settings dialog each get `overflow-y: auto` so 200% text zoom reflows/scrolls internally
  instead of clipping (the board viewport is the only fixed-fit element). Verified by
  `apps/web/e2e/smoke.spec.ts`'s 658×320 (smallest supported landscape) zoom pass.
- **Player-started runs**: a fresh run/Play-again holds at tick 0 (no hidden countdown) until
  the player presses **Start**; Space/speed changes never un-hold. Verified end-to-end by the
  new `apps/web/e2e/start-gate.spec.ts` against the board's own `data-run-started`/
  `data-sim-tick`/`data-pending-adds` test-hook attributes, rather than inferred from a wait.

**New modal states recorded:** a single modal owner (`modal.ts`) now owns `inert` on
`.wy-shell` and focus save/restore for a `results > rotate > settings` priority stack — only
the highest-priority open overlay is shown/focused, and `Escape` is consumed by the stack
before any game-level Escape handling. Settings migrated from an inline panel to a bounded,
scrollable dialog with its own labelled Close button.

**New rotate state recorded:** `.wy-rotate` (sibling of the Shell) shows on
`(orientation: portrait)` **and** `(pointer: coarse)` both matching (phones/tablets only — a
narrow desktop window merely squeezes). Entering portrait aborts any in-flight placement
gesture and auto-pauses an active, unpaused run; returning to landscape closes the overlay
but the run **stays paused** until the player explicitly presses Resume — nothing
auto-resumes. Verified by the new `apps/web/e2e/rotate.spec.ts` (`chromium-touch` project,
a real `devices['Galaxy S9+ landscape']` profile), including an axe audit and a rendered-
contrast spot check on the overlay itself.

**New e2e coverage + config landed this story:**

- `apps/web/playwright.config.ts`: a `chromium-touch` project on a real mobile landscape
  device profile (`hasTouch` alone does not guarantee `(pointer: coarse)` matches, which the
  gated behavior below depends on), scoped to `touch.spec.ts`/`rotate.spec.ts`; the default
  `chromium` project's `testIgnore` extended to also skip them (plus `hidpi.spec.ts`, as
  before).
- `apps/web/e2e/start-gate.spec.ts` (new) — the held-at-tick-0 gate, a Pending pre-start
  build, Start, and the Play-again round-trip.
- `apps/web/e2e/touch.spec.ts` (new) — press-adjust-release (with a real rendered-pixel
  proof the commit lands at the offset anchor, not the finger cell, via a small CDP
  `Input.dispatchTouchEvent` driver since Playwright's `Touchscreen` API only supports taps),
  Card tap-vs-drag, chrome/off-board drag cancellation, invalid-tap-keeps-armed, and an axe
  audit of the armed Panel under touch.
- `apps/web/e2e/rotate.spec.ts` (new) — the portrait/coarse gate, auto-pause, the
  stays-paused-until-Resume invariant, and an axe + contrast audit of the overlay.
- `apps/web/e2e/smoke.spec.ts` additions — full-keyboard Card arm + place (the `armTower1`
  hotkey, not just a mouse click), an additional settings focus-restoration angle
  (focus-then-Escape), the `aria-disabled` Upgrade control's keyboard reachability, rendered
  contrast on the Card/Panel/Dock, and the 658×320 200%-zoom reachability pass described above.

**Real-device items explicitly deferred to #36** (this story's natural follow-up playtest,
per PLAN.md's "Risks / open questions"): smallest-phone playability (the always-fit board
gives ~12px cells on the smallest phones — a hypothesis this checklist's synthetic e2e
coverage cannot settle) and synthetic touch fidelity (Playwright/CDP touch events are a good
approximation of a finger, not a replacement for one). Neither is claimed as verified by this
audit.

**Re-audited rows — status confirmed:** every ✅ row in the GAG §2 table above already
reflects Story 10's current behavior (edited in place, not left as stale Story 6/7 prose) —
"Full functionality across touch/mouse/keyboard", "Remappable / rebindable controls",
"DOM/HUD text resize to 200%", "Pause", "Contrast ≥ 4.5:1", and "Semantic controls / labels
for AT" all now describe the Armed/press-adjust-release/Shell/modal-owner/rotate model
directly rather than the pre-Story-10 two-tap/inline-settings/hidden-timer one.

---

## Story 11 conformance audit — Mobile screen estate (M1-S11: Compact layout, install path, fullscreen)

Story 11 reclaims phone screen estate. It adds a **Compact layout** on short viewports, an
honest **install path**, and **fullscreen-on-Start** where the platform supports it. No
sim/engine/content changes. New and changed accessibility-relevant surfaces:

| Surface                                    | Status | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ------------------------------------------ | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **Compact layout** (`(max-height: 500px)`) | ✅     | The status header becomes a full-height left COLUMN: dual-form chips (the complete localized ICU message stays the accessible text in BOTH layouts; the visible glance form — icon + value — is `aria-hidden`, so AT never hears a value twice and no label is ever sentence-split), above an in-column Dock whose buttons keep their localized accessible names with only their VISIBLE text hidden. Column and rail widths are vw-capped so 200% text zoom cannot starve the board; the chips list is a labelled, keyboard-reachable scrollport, and every Dock control stays focusable and in-viewport at 200%. Board floors are pinned numerically against the projected playable grid (`compact.spec.ts`, both the `chromium` and `chromium-touch` projects) rather than eyeballed. The Card's hotkey badge is hidden on Compact — a SPACE decision, not a capability one: touch laptops in a short window keep the keyboard guidance everywhere else, and the binding itself is unchanged and still discoverable in Settings.                                                                                                                                                                                                                                                        |
| **Settings dialog rename**                 | ✅     | The dialog is now "Settings" (it holds more than accessibility); the accessibility controls keep their own "Accessibility" heading, so they remain a named group rather than the dialog's unlabelled remainder. Heading order is h2 → h3 → h3.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **Install banner**                         | ✅     | A reserved chrome ROW, never an overlay covering the board — it is disjoint from the playable grid, and the board keeps a pinned floor with it visible. Real `<button>`s; the glyph-only dismiss control carries a localized `aria-label` and a ≥44px target. Shown only pre-start, once per session, and never after the first Start. Every visibility change re-homes contained focus (to the Dock's Start button) rather than dropping it on `document.body`, and cancels any in-flight placement gesture — the row resizes the stage. Axe audited with the banner visible.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **Add-to-Home-Screen instructions dialog** | ✅     | A first-class labelled `role="dialog"` on the same modal owner (`settings` priority), so it inerts the Shell and saves/restores focus like every other modal. Escape dismisses it — Escape behaviour is now per-overlay metadata rather than a hardcoded priority check, which is what let a second dismissable dialog exist at all. Opening it from Settings closes Settings first, so the two never share the stack; closing it returns focus to the Settings opener. Axe audited while open.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Dock focus order** (WCAG 2.4.3)          | 🟡     | The Dock is now a child of `header.wy-status` in BOTH layouts (one DOM topology, two layouts), so it is tabbed straight after the status chips and BEFORE the board. Compact matches paint order exactly (left column: chips, then Dock, then board to their right). **Standard does not:** the Dock is painted bottom-left over the Stage, so a Standard keyboard user reaches it one stop before the board it floats over. **Ratified — accepted as-is (product owner, 2026-07-25):** the Dock is a four-control labelled cluster reached immediately after the chips — never a detour past unrelated content — and it keeps the "chrome before content" order the wordmark and chips already establish, which WCAG 2.4.3 permits (meaning and operability preserved, not strict visual correspondence). The contract-amendment alternative was declined: it merely relocates the DOM/paint mismatch to Compact. Gated by `compact.spec.ts`'s "the chips list is a Standard keyboard stop, ahead of the Dock controls" so the order cannot drift unnoticed, and cross-referenced from `shell.ts` (`status.append`) and `ui.css` (`.wy-dock`). Revisit trigger: issue #36's real-device playtest, or if the Dock grows beyond a small cluster or Standard gains a second floating region. |
| **Fullscreen on Start**                    | ✅     | Requested only on the `started` false→true edge, and only when `requestFullscreen` exists ∧ the pointer is coarse ∧ the app is not already standalone ∧ the document is not already fullscreen — capability-based, never UA sniffing. **It never traps:** the app requests fullscreen and then leaves it entirely alone. Nothing here calls `exitFullscreen`, nothing listens to `fullscreenchange`, and no game state depends on it — the platform's own exits (the system back gesture, Escape, swiping down the notification shade) work exactly as the player expects, and leaving fullscreen mid-run changes nothing about the run. A refusal is swallowed: Start behaves identically either way. The rotate overlay keeps working inside fullscreen (it is a DOM sibling, not a browser affordance).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |

**Verification note (fullscreen):** real fullscreen is not CI-exercisable — headless Chromium
either refuses the request or resizes the viewport out from under every geometry assertion in
the suite. Every touch-project spec that presses Start therefore applies a resolving spy
(`apps/web/e2e/fullscreen-stub.ts`), with one dedicated test asserting the request fires
exactly once under the gate and is not repeated mid-run, and a fine-pointer counterpart
asserting it never fires. All eight enumerated gate branches are unit-covered
(`fullscreen.test.ts`, `main.test.ts`).

**Real-device items still deferred to #36:** smallest-phone playability at the pinned floors
(568×320 gets ~10px cells — the supported floor by construction, not a claim that it plays
well), and whether the Compact column's glance chips read as clearly on a real phone as they
do at desktop scale. Neither is claimed as verified by this audit.

---

## Home affordance conformance audit — the site home link

`/play` gains exactly one link back to the Wynding site: the board-mark plus the game's
existing wordmark, in the status row's first slot, auto-hidden while a run is live. No
sim/engine/content changes. New and changed accessibility-relevant surfaces:

| Surface                                       | Status | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| --------------------------------------------- | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Home link** (`a.wy-home`)                   | ✅     | A real `<a href="/">`, not a scripted button — modified activations (cmd/ctrl/shift/alt, middle-click) are never intercepted, so open-in-new-tab keeps working. Its accessible name is an externalized catalog string (`app.home`) on the anchor; the inline board-mark is `aria-hidden` decoration with no name of its own, and the anchor carries no `title` (a `title` matching the `aria-label` becomes the accessible DESCRIPTION, so AT would read the same string twice — the hover affordance is a CSS surface tint instead, which also works for touch). AT hears the link exactly once. The wordmark span was UPGRADED into it rather than joined by a second element, so there is still one "Wynding" in the bar. Hit target ≥44×44 CSS px in BOTH layouts, hit-tested at all four corners plus the centre and asserted disjoint from the projected playable grid (`home.spec.ts`), including at 200% text zoom on the narrowest supported viewport. Standard reaches the 44px floor by spending the status row's own padding via bounded negative margins (net row growth ≈3.4px, not the ≈19px a naive `min-height` item would cost); Compact absorbs it in the in-flow chrome column, so the board loses nothing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **Home link focus order** (WCAG 2.4.3)        | ✅     | The link is the Shell's FIRST tab stop in both layouts, ahead of the chips scrollport and the Dock — conventional "skip to site" chrome-first ordering, and it matches paint order in both (top-left of the Standard row; top of the Compact column). Unlike the Dock row above, there is no DOM/paint mismatch to accept here.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **Auto-hide while live** (WCAG 2.1.1 / 2.4.3) | ✅     | Hidden for any started-and-unpaused moment; visible held pre-start, while paused, and once the run resolves. Interactivity is removed **synchronously and completely** on the flip — the `inert` attribute (dropping the tab stop and every activation path) and `pointer-events: none` land in the same update as the state change, so there is never an interactable-but-invisible ghost during the fade; only `opacity` animates, with `visibility: hidden` following at the fade's end. Every path that can flip the state outside a tick — Start, all five pause callers, Play-again — refreshes in its own handler rather than relying on a frame, so a throttled background tab can never strand a stale state; the terminal transition needs no handler because a run can only resolve on a tick, inside a frame that refreshes anyway. The fade is dropped when EITHER `prefers-reduced-motion: reduce` matches (WCAG 2.3.3) OR the in-app Reduce-motion setting is on. Focus is never stranded inside the link when it hides: hiding only follows Start/resume actions whose focus lands elsewhere, and both hide-path tests assert `document.activeElement` is outside the link rather than assuming it. The Compact focus ring is drawn with a negative outline offset so the column's `overflow: hidden` cannot clip its vertical segments; verified by sampling rendered pixels, since neither the DOM nor axe can see ring clipping.                                                                                                                                                                                                                                                                                       |
| **Terminal is orientation-only**              | ✅     | The link is visible once a run resolves, but the results dialog is deliberately non-dismissible and inerts the whole Shell, so it is branding/orientation rather than a live control until Play-again returns the run to held — at which point it is asserted actionable again. Making Results dismissible was raised and declined: it is a product change to the terminal flow, out of this story's scope.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Leave-run confirm dialog**                  | ✅     | A plain left-click opens a labelled `role="dialog"` whenever the run is unresolved AND there is something to lose — a started run, or a held one with a pending pre-start plan (PLAN.md Amendment 1: a held run is not necessarily empty, and losing a planned maze with no warning is the same loss the dialog exists to prevent). It is on the same modal owner at `settings` priority (rotate and results both still outrank it), so it inerts the Shell and saves/restores focus like every other modal — never `window.confirm()`, which is neither localizable nor stylable and bypasses that machinery entirely. The dialog body is wired as the dialog's `aria-describedby`, so the consequence is SPOKEN on open rather than left for a player to discover by browsing — without it a screen reader announces only the title and the focused button, and the one irreversible action in the app would be explicit only to people who can see it (axe does not flag a missing description). Initial focus is on **Stay**, the safe action, and Escape dismisses to the same place, so no accidental keypress can end a run. The guard pauses the run before opening (belt-and-braces — it does not depend on the link being unreachable while unpaused) and focuses the link first so focus restore on Stay is deterministic across browsers, since Safari does not focus anchors on click. Containment is the owner's inert-plus-hidden-siblings mechanism, **not** a focus trap this app implements: tabbing past the dialog's last control hands focus to the document and browser chrome, never to anything behind the dialog — verified as such in Playwright (both directions), alongside an axe scan with the dialog open. |

---

## Story 3 conformance audit — the slow + fast creep picker (M2-S3)

M2-S3 turns the single-Card Rail into a picker: a second buildable tower (`slow`, a ground
tower that direct-damages then applies a slow status effect) joins `basic`, and a new fast
creep joins the wave 3 composition. No layout/topology change — the Rail gains a second
`.wy-card`, in catalog order, sharing the existing Card/Panel/Dock/hotkey machinery. New and
changed accessibility-relevant surfaces:

| Surface                                               | Status | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ----------------------------------------------------- | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **The picker: two Cards, keyboard + AT path**         | ✅     | The Rail builds one Card per catalog tower (`shell.ts`), same DOM per card as the M1 single-Card shape: a whole `button.wy-card[aria-pressed]` with localized name/cost/hotkey spans. `armTower1`/`armTower2` (Digit1/Digit2 by default) arm catalog index 0/1 from document scope, "any state" — unchanged from the M1 arming contract, just widened to two slots. Switching arms (basic → slow on the same cell) re-evaluates placement validity fresh (the aim-memo key now includes the armed tower id) rather than leaking a stale verdict, so the ghost/Panel/live region never lie about which tower a switch actually prices. A card at catalog index ≥ 2 (a modded bundle; sv7 compiles up to 64 towers) gets NO hotkey badge/`aria-keyshortcuts` at all — but stays fully keyboard-operable as a native button in tab order, never removed from the Rail. Conversely, a legal one-tower bundle FILTERS `armTower2` out of the Settings rebind list (no phantom rebindable action for a slot that doesn't exist) and its document hotkey no-ops. Unit-tested: `overlay.test.ts`, `controller.test.ts`, `keymap.test.ts` (QC round-1 #8: `input.test.ts` has no `armTower2`-specific test — that false citation is what let finding 7 through — dropped here); e2e: `smoke.spec.ts`'s second-card path (click AND `Digit2`, axe-clean with the Rail as it stood at Story 3 — that same spec now exercises the 3-card Rail, per the Story 4 row below), `touch.spec.ts`'s tap-arms-second-card + drag-switch-from-slow-Card.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Per-card touch targets (≥ 44×44 CSS px)**           | ✅     | Every `.wy-card` — now two of them — keeps the `.wy-btn`-family sizing floor (`min-height: 44px`, full Rail width, `ui.css`), so the second Card meets the same GAG §2 target-size bar the single M1 Card did; Compact's vw-capped Rail width never squeezes a card below the floor (the cards are full-width children of the Rail, whose minimum exceeds 44px by construction). Hit-target geometry is exercised by `touch.spec.ts`'s per-card tap/drag paths on the real device profile.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **Tower/creep names — open catalog, never a raw id**  | ✅     | `TOWER_NAME` is a PARTIAL literal-key map (`basic`, `slow`) with a localized `tower.unknown.name` (`{id}`) fallback + dev-mode console warning — the exact `CREEP_NAME` strategy this checklist already covers for creeps (Story 7). Catalog ids are OPEN strings (any schema-valid direct+slow tower compiles at sv7), so a closed union/exhaustive switch would crash on a legitimate modded bundle; AT and sighted players alike always get a real, localized name or the honest "Unknown tower (id)" fallback, never a raw catalog string. `CREEP_NAME` gains `fast`. Both maps are unit-tested for the fallback + warning path.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **The slowed telegraph — dual-encoded, canvas-only**  | ✅     | The slowed status effect is drawn on a creep with a shape cue that is ALWAYS present (an overlay ring in `palette.slowed`, ≥ 3:1 contrast against the board floor like every other opaque cue — `palette.test.ts`) plus a motion cue — a wider ring whose radius genuinely OSCILLATES with render time (driven by the same `renderTimeOf` clock the Tracers use, one full breath per 900 ms) — that YIELDS to reduced motion — the same posture the impact spark and Tracer already take (WCAG 2.3.3 / GAG §2). Colour is never the sole channel: an unslowed vs. slowed creep differ in silhouette-adjacent geometry, not just in an added tint. **CORRECTION (M2-S5a, Codex review of PR #78): this row was ✅ from M2-S3 for a cue that did not render.** The executor passed the paint plan the creep's raw `CreepVM.x`/`y`, which are FIXED-POINT sim units (256 per cell) rather than the projected pixel centre the silhouette uses — so the ring and pulse drew roughly 256× off-position, off-canvas, for the whole of M2-S3 and M2-S4a. No player saw the slowed telegraph in that window. It is fixed here, and `scene.test.ts` now asserts both telegraph plans receive the projected centre. Why the ✅ survived two milestones: the supporting evidence was a pure paint-plan unit test called with unit-agnostic literals (`{x: 0, y: 0}`), which is structurally incapable of witnessing a call-site units bug — every other canvas-only ✅ on this checklist whose sole evidence is a paint-plan test carries the same blind spot and is worth re-auditing. The telegraph lives entirely on the Phaser canvas (axe-invisible by construction — ADR 0003 §3), so its accessible story is this dual-encoding itself, not a DOM/AT surface: there is deliberately NO per-application live-region announcement for a slow landing (combat chatter would flood AT — the existing "HUD is not a live region" rationale extends to per-hit status effects). The paint-plan geometry (ring/pulse radii, reduced-motion branch) is unit-tested at the REAL supported floor (`creep-paint.test.ts`, `CELL_PX_MIN_NARROW` = 10px cells, 568×320 — not the 12px the risk-of-record originally assumed) — legible ≥ 1px distinct strokes at the narrowest board a player will ever see this on. **Reduced-motion behavior verified via the paint-plan unit tests** (`creep-paint.test.ts`: with reduced motion on, the plan emits the ring only, time-invariant; with it off, the pulse radius provably differs across render times — motion that is real, not a static second ring); a MANUAL on-device pass (OS setting + in-app toggle) has NOT been performed in this story and is deferred to the same #36 follow-up playtest as every other canvas-only item on this checklist. |
| **Creep silhouette — id-keyed shape**                 | ✅     | `fast` gets a diamond silhouette, visibly distinct from `normal`'s triangle at cell scale — a second shape cue (independent of the slowed telegraph above) so a fast creep reads as a different kind of threat without relying on colour or reading its HP pip. An unrecognized creep id (a forged/future content id) falls back to the triangle rather than throwing — total, matching the tower footprint mark's same posture below. Unit-tested (`creep-paint.test.ts`) at the narrow-floor cell size.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **Tower footprint mark — id-keyed, colour-redundant** | ✅     | `slow` and `basic` share `palette.tower`'s colour (M2-S3 deliberately does not mint a second tower colour); the distinction is carried entirely by an added inner ring mark on `slow`'s footprint — shape, not colour, per ADR 0003's redundant-encoding rule. Threads through the FULL pending-build pipeline: a slow tower pending while paused keeps its shape-distinct pending outline too (`RenderOverlay.pendingAdds` carries `towerId`, Codex R1-7) — a player reviewing a paused plan can tell which pending tower is which before it commits. Unit-tested (`tower-paint.test.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Panel stats — per-tower, data-driven**              | ✅     | The Panel's stat rows (cost/damage/range/fire-rate/targets) are now derived from the armed/selected `CompiledTower` rather than a hardcoded M1 constant — `slow`'s damage row correctly reads its (lower) direct-effect total, not `basic`'s. No separate accessibility surface change (same `<p>` rows, same `t()` catalog keys), but the wrong-stats failure mode this replaces (a stale/incorrect number reaching AT and sighted players identically) is regression-tested (`overlay.test.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **Announcements name the correct tower**              | ✅     | The live region's armed/placed messages (`live.armed`, `live.placed`) now interpolate the ACTUAL armed/placed tower's localized name via `TOWER_NAME`, not a hardcoded "Basic Tower" — arming or placing the slow Card announces "Slow Tower armed. Place it on the board." / "Slow Tower placed.", matching what a sighted player sees on the Card and Panel. Regression-tested (`overlay.test.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |

**No status-effect wording drift:** every surface above uses the canonical glossary
terms **status effect** and **telegraph**. The `check:glossary` gate enforces the avoided
words over its include set (CONTEXT/vision/roadmap/prd/milestones — this checklist is NOT
in that set); here the discipline is editorial, held by review.

**Real-device items still deferred to #36** (this story adds nothing new to verify there
beyond what Story 11 already deferred): the slowed telegraph's ring/pulse legibility at the
narrow floor is asserted by the unit paint-plan test, not a real-device capture.

---

## Story 4 conformance audit — AoE + swarm, direct damage's area form (M2-S4a)

M2-S4a adds `splash` (a `ground` tower whose bundle authors an `aoe` effect — a **blast**:
a point-locked impact that leads its target along the creep's path at fire-time speed, then
damages every creep within its radius) and `swarm` (16 fragile creeps at wave 2, the AoE
showcase), plus a third Card in the Rail. The mechanic is decorative on the canvas — outcomes
are always carried by HP pips, per the Tracer/Blast glossary entries — so the accessibility
obligation is dual-encoding (shape, never colour alone), reduced-motion compliance, and the
blast radius **as text**, never ring-only. New and changed accessibility-relevant surfaces:

| Surface                                                                     | Status | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| --------------------------------------------------------------------------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **The picker: three Cards, keyboard + AT path**                             | ✅     | `armTower3` (Digit3 by default) joins `armTower1`/`armTower2` in the `GameAction` union, `DEFAULT_KEYMAP`, `input.ts`'s exhaustive switch, the settings rebind list (catalog-gated, same as `armTower2`), `overlay.ts`'s label map, `ARM_HOTKEY_ACTIONS`, and the hotkey badge/`aria-keyshortcuts` surface — the same widened-not-redesigned path Story 3 established for the second Card. Unit-tested: `overlay.test.ts`, `controller.test.ts`, `keymap.test.ts` (QC round-1 #8: `input.test.ts` has no `armTower3`-specific test — dropped here, same false-citation pattern as the second Card's row above); e2e: `smoke.spec.ts`'s third-card path (click AND `Digit3`, axe-clean for armed/valid-placement/invalid-placement), `compact.spec.ts`'s three-badge Compact-hidden pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **Tower/creep names — open catalog, never a raw id**                        | ✅     | `TOWER_NAME` gains `splash`, `CREEP_NAME` gains `swarm` — the same partial literal-key map + localized `tower.unknown.name`/generic-fallback strategy Story 3 covers; AT and sighted players alike get a real, localized name or the honest fallback, never a raw catalog string.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **Panel `damage` stat — AoE-inclusive (regression fixed)**                  | ✅     | `towerStats`' damage sum previously read only `kind === 'direct'` effects — an AoE-only tower like `splash` would have rendered "Damage: 0" to every player, sighted or AT, despite its `aoe` effect carrying real damage (found and fixed in Phase 2's wiring pass, before this story's canvas work began). The sum now reads `direct` OR `aoe` — both are direct damage's two forms (CONTEXT.md's **Effect primitive** entry) — so the stat a player reads is never silently wrong. Regression-tested (`overlay.test.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **Blast radius — labelled as TEXT (never ring-only)**                       | ✅     | The Panel's `panel.blastRadius` stat row (`{tiles} tiles`) shows whenever the armed/selected tower has an `aoe` effect, alongside the pre-existing `panel.range` row that every tower already shows — the exact number is always readable as text, independent of whether a player perceives the canvas rings at all (WCAG: no essential information by shape/colour alone). Unit-tested (`overlay.test.ts`); e2e-asserted for both the armed AND the just-placed/selected Panel states (`smoke.spec.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Armed-splash ghost's blast-radius preview — shape-distinct, canvas-only** | ✅     | The ghost already draws an attack-range ring (`pal.range`, a smooth circle); a second plain circle at the same footprint for the blast radius would be genuinely ambiguous (is the inner one the splash, a permanent aura, or the range?). The blast-radius preview instead draws four short radiating spokes (`drawCrosshair`, `scene.ts`) — a shape distinct from the smooth range circle, never colour alone — at the tower's true `blastRadiusFp` (`GhostVM.blastRadiusFp`, `null` for a single-target tower). Purely decorative: the Panel's text row above carries the exact number regardless. Lives entirely on the Phaser canvas (coverage-excluded, axe-invisible by construction — ADR 0003 §3); its geometry/existence is proven by `controller.test.ts` (armed-splash ghost carries the compiled `aoe` radius; a single-target armed tower's ghost carries `null`) and by `smoke.spec.ts`'s valid/invalid-placement + reduced-motion e2e passes (DOM/axe stability, not the ring's pixels).                                                                                                                                                                                                                                                                                 |
| **Blast landing — a dual-encoded, reduced-motion-damped ring**              | ✅     | A blast's resolution point (`StepEvents.impactPoints`, `radiusFp` now threaded through `RenderOverlay.sparks`) draws a ring at the blast's true radius — expanding-and-fading at normal motion, static-and-fading under reduced motion — (`scene.ts`'s `drawSparks`), instead of the small fading dot a `targeted` impact draws (`radiusFp === 0`) — a shape/behaviour distinction, not colour alone (both use `pal.spark`). Under reduced motion the ring HOLDS ITS FULL RADIUS and only fades (`grow` clamped to 1) — NOT the spark's posture, which round 1 wrongly claimed here: merely shortening `life` while still sweeping 0→radius would COMPRESS a multi-tile expansion into a shorter window, i.e. faster motion over a larger area (WCAG 2.3.3 / GAG §2). Purely decorative: the kill/damage outcome is always carried by the HP pip, per the Tracer/Blast glossary entries — a player who never perceives this ring loses no essential information. Canvas-only (ADR 0003 §3, coverage-excluded); its existence/threading is proven by `controller.test.ts` (a splash kill's drained spark carries the compiled AoE radius, not 0) — the ring's own pixel geometry is not independently unit-tested (mirrors the pre-existing spark's own posture, which never was either). |
| **`swarm` creep silhouette — id-keyed shape**                               | ✅     | `swarm` draws a small square, visibly distinct from `normal`'s triangle AND `fast`'s diamond at cell scale — a third shape cue in the same `creepShapeFor` total-over-any-string map (an unrecognized id still falls back to the triangle). Unit-tested (`creep-paint.test.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **`splash` tower footprint mark — id-keyed, colour-redundant**              | ✅     | `splash` shares `palette.tower`'s colour with `basic`/`slow` (this story mints no third tower colour); the distinction is a four-spoke `'crosshair'` mark — the `TowerFootprintMark` vocabulary widens from `'plain' \| 'ringed'` to add `'crosshair'` rather than reusing `'ringed'` (already `slow`'s), so no two towers share a footprint mark. The SAME spoke motif the ghost's blast-radius preview draws at cell scale, reinforcing one consistent "area effect" shape language. Threads through the pending-build pipeline exactly like `'ringed'` did at Story 3 (a pending splash tower keeps its shape-distinct pending outline). Unit-tested (`tower-paint.test.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Wave 2 preview — swarm's composition, sighted + AT**                      | ✅     | The wave preview (Story 6/S2's own DOM surface, never chip-hosted) reads wave 2's `swarm × 16` from the compiled `entriesSummary` exactly like every other wave — no separate accessibility surface change, since the preview is already fully data-driven per-wave. Covered by the existing wave-preview axe pass (`smoke.spec.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **Third-Card stale test counts — a pre-existing regression, fixed here**    | ✅     | Phase 2's `armTower3` wiring landed the third `.wy-card` and its hotkey badge in the actual DOM, but left three e2e assertions pinned to the old two-Card/two-badge counts (`smoke.spec.ts`'s app-shell test, `compact.spec.ts` ×2 for the Standard/Compact hotkey-badge passes) — all three were silently red before this pass (verified: reverting the count fix reproduces the failure). Corrected to 3; no other stale count found across the e2e suite (`touch.spec.ts` selects Cards by accessible name, never by index).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |

**No status-effect wording drift:** the table above uses the canonical glossary terms
**Impact**, **Blast**, and **Tracer** (this story's grill-with-docs session added the first
two to `docs/CONTEXT.md`) — held by review, same as Story 3's discipline.

**Real-device items still deferred to #36** (this story adds nothing new to verify there
beyond what Story 11/Story 3 already deferred): the blast-radius crosshair preview's and the
blast-landing ring's legibility at the narrow floor is asserted only by the surrounding unit
tests (`controller.test.ts`'s radius/spark-payload plumbing) and by e2e's DOM/axe stability —
not a real-device capture, and not a dedicated narrow-floor paint-plan unit test the way the
Story 3 slowed telegraph got one (`creep-paint.test.ts`'s `CELL_PX_MIN_NARROW` pin). `scene.ts`
itself stays coverage-excluded by long-standing convention (not meaningfully testable under
jsdom); this is the same posture the pre-existing impact spark and range ring already carry,
extended to the new blast ring and blast-radius preview rather than a new gap this story
introduces.

---

## Story 5 conformance audit — armor + DoT: `venom` and `armored` (M2-S5a)

M2-S5a adds armor (a `direct`-damage-reducing stat every creep now carries, `armored`'s is 6) and DoT (`dot`, an armor-bypassing damage-over-time effect `venom` authors) — plus a
fourth Card in the Rail and wave 4's `armored` creep. Armor itself is dimensionless
(a `direct` hit is simply reduced before the HP pip moves, per the existing dual-encoded
pip), so its only NEW accessibility surface is the wave preview's per-creep `armor {n}`
text, which now reads a genuinely nonzero number for the first time. A live DoT record,
however, is armor-bypassing damage already SCHEDULED that no other surface reveals —
HP pips show damage already TAKEN, not damage still to come — so PLAN.md step 32 gives the
DoT telegraph the SAME essential-cue posture the Story 3 slowed telegraph already
established: a guaranteed shape cue on canvas, a reduced-motion-safe motion cue, contrast
and geometry tests, and (recorded explicitly, both here and at the telegraph's own
definition in `creep-paint.ts`) a deliberate absence of any per-tick live-region
announcement — a DoT record can tick every few frames for its whole duration, and
announcing each one would flood a screen reader with noise carrying no new decision for
the player; the _state_ (a creep is currently poisoned) is what the always-on shape cue
already carries. New and changed accessibility-relevant surfaces:

| Surface                                                                     | Status | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| --------------------------------------------------------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **The fourth Card: keyboard + AT path**                                     | ✅     | `armTower4` (Digit4 by default) joins `armTower1`–`armTower3` — the same widened-not-redesigned path each prior Card followed, landed generically for slots 1..9 in P6. Unit-tested: `overlay.test.ts`, `controller.test.ts`, `keymap.test.ts`; e2e: `smoke.spec.ts`'s fourth-card path (click AND `Digit4`, axe-clean for armed/valid-placement/invalid-placement — mirrors the third Card's own row above), `compact.spec.ts`'s four-badge Compact-hidden pass (already pinned to 4 from P6).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Tower/creep names — open catalog, never a raw id**                        | ✅     | `TOWER_NAME` gains `venom`, `CREEP_NAME` gains `armored` — the same partial literal-key map + localized `tower.unknown.name`/`creep.unknown.name` fallback strategy every prior story's row above covers.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Panel DoT stat — magnitude, cadence, duration, all as TEXT**              | ✅     | `appendStatRows` gains a `panel.dot` row (`"Poison: {damage} damage every {cadence}s for {duration}s"`), shown whenever the armed/selected tower has a `dot` effect and omitted entirely otherwise (`stats.dot === null`, mirroring `blastRadiusTiles`'s null-omission posture exactly — a tower with no `dot` effect renders no row, never a `0`). The exact scheduled damage is always readable as text, independent of whether a player perceives the canvas telegraph at all (WCAG: no essential information by shape/colour alone) — the SAME principle the blast-radius text row established at Story 4. Unit-tested (`overlay.test.ts`); e2e-asserted for both the armed AND the just-placed/selected Panel states (`smoke.spec.ts`), exact string `"Poison: 4 damage every 0.5s for 3.0s"` (M2-S5b PR A: `venom`'s DoT moved 2 → 4 per tick, the wording pattern unchanged). **The row deliberately avoids the word "tick"** (QC round 3): `docs/CONTEXT.md` reserves it for the 50 ms simulation step, so `"{damage}/tick every {cadence}s"` used the term in a second, conflicting sense within one sentence — a player applying the glossary would read 4 × 20 = 80 DPS against the true 8. Since this row is the sole channel for the schedule when the canvas telegraph is not perceived, an ambiguity here is an accessibility defect, not a copy nit. Do not reintroduce "tick" into player-facing stat text.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **The DoT telegraph — dual-encoded, canvas-only, ESSENTIAL not decorative** | ✅     | A poisoned creep draws a shape cue that is ALWAYS present — three pips in `palette.poisoned` at `r×1.8` from the silhouette centre, ≥ 3:1 contrast against the board floor like every other opaque cue (`palette.test.ts`) — plus a motion cue: the same three pips additionally drift outward and fade (driven by render time) — that YIELDS to reduced motion (WCAG 2.3.3 / GAG §2), the exact posture the slowed telegraph already takes. Unlike the slowed ring, this is explicitly NOT decorative-by-analogy (PLAN.md step 32, correcting an early draft's reasoning): HP pips show damage already taken, a DoT record is damage already scheduled, so the shape cue carries information no other surface does. Sits at `r×1.8`, deliberately OUTSIDE the slowed ring's `r×1.4`, so a creep carrying BOTH statuses reads as two distinct concentric cues rather than a muddle — asserted directly (`creep-paint.test.ts`). Colour is never the sole channel. The telegraph lives entirely on the Phaser canvas (axe-invisible by construction — ADR 0003 §3), though its draw calls are NOT coverage-excluded: QC round 1 found the paint layer entirely unwitnessed, so `drawCreeps`/`drawTowers` were extracted into the Phaser-free `board-draw.ts` and are now asserted against a recording `GraphicsLike` double (`scene.test.ts`) — deleting the pip loop, the hexagon branch or the droplet mark now fails a test. There is deliberately NO per-application live-region announcement for a DoT tick — see this section's opening paragraph and the rationale recorded directly at `dotTelegraphPaintOps` in `creep-paint.ts`. The paint-plan geometry (pip/drift positions, reduced-motion branch, the outside-the-slow-ring composition) is unit-tested at the REAL supported floor (`creep-paint.test.ts`, `CELL_PX_MIN_NARROW` = 10px cells), same as the slowed telegraph's own gate. `palette.poisoned` (`0xcc79a7` Okabe-Ito reddish purple, `0xf0e442` in tritan) is gated in `palette.test.ts`'s permanent contrast suite as an essential opaque cue, the same category as `slowed`, AND by a new pairwise gate requiring it to differ from every cue a pip can be drawn over. Both gates were added after QC round 1 found the first draft's `0x009e73` byte-identical to `tower`/`ghostValid`, which made the pips vanish on the tower bodies creeps path across. The pairwise gate is byte-equality only — it cannot measure perceptual distance under simulated CVD, and round 2 noted the tritan value sits closer to that mode's `creep` than to its other cues; the always-on shape cue is what carries the state there, colour being the redundant channel. |
| **`armored` creep silhouette — id-keyed shape**                             | ✅     | `armored` draws a six-sided hexagon, visibly distinct from `normal`'s triangle, `fast`'s diamond, AND `swarm`'s square at cell scale — reading as plated/armoured. A fourth shape cue in the same `creepShapeFor` total-over-any-string map (an unrecognized id still falls back to the triangle). Unit-tested (`creep-paint.test.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **`venom` tower footprint mark — id-keyed, colour-redundant**               | ✅     | `venom` shares `palette.tower`'s colour with `basic`/`slow`/`splash` (this story mints no fourth tower colour); the distinction is a small teardrop `'droplet'` mark — the `TowerFootprintMark` vocabulary widens to add `'droplet'` rather than reusing any existing value, so no two towers share a footprint mark. Threads through the pending-build pipeline exactly like every prior mark. Unit-tested (`tower-paint.test.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **Wave 4 preview — the FIRST nonzero armor text, sighted + AT**             | ✅     | Every creep before this story compiled with `armor: 0`, so the wave preview's `hud.preview.armor` text (`"armor {n}"`, PLAN.md P3 step 17) has always read `armor 0` for every entry, on every wave, in every prior story. Wave 4's `armored × 6` is the first entry in the game's history to compile a nonzero value, and the preview reads it correctly as `armor 6` with no separate code change (the preview was already fully data-driven per-creep) — but it is the first REAL exercise of that text path, so it is asserted explicitly — in e2e, which is the only place the real wave-4 preview is exercised: `smoke.spec.ts`'s full-run test pins `EXPECTED_COMPOSITION[4]` = `'6 × Armored Creep — ground, armor 6, no immunities'`, and its reduced-motion test asserts that same row after early-calling through to wave 4. **QC round 3 corrected this cell's citations**, which named two pieces of evidence that do not exist: `overlay.test.ts` has no `armored` case at all (its only nonzero-armor rows are the pre-existing `armor: 2` unknown-creep fallbacks from an earlier story, and this PR did not touch its preview tests), and the new fourth-Card e2e flow never queries `.wy-wave-preview`. The ✅ stands on the two real assertions; only the pointers were wrong — the exact "cites evidence a maintainer cannot find" pattern this document flags twice as what let finding 7 through.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |

**No status-effect wording drift:** the table above uses the canonical glossary terms
**Telegraph** and **Impact**; DoT/`dot` and armor are used as CONTEXT.md/m2.md already
define them — held by review, same as every prior story's discipline.

**Real-device items still deferred to #36** (this story adds nothing new to verify there
beyond what Story 3/4/11 already deferred): the DoT telegraph's pip/drift legibility at the
narrow floor is asserted only by the paint-plan unit tests (`creep-paint.test.ts`) and by
e2e's DOM/axe stability, not a real-device capture — the same posture the slowed telegraph
and the blast ring both already carry. `scene.ts` itself stays coverage-excluded by
long-standing convention (not meaningfully testable under jsdom).

---

## Story 6 conformance audit — stun + immune counterplay: `stun` and `resolute` (M2-S6)

M2-S6 adds `stun` (a `ground` tower whose bundle authors a `stun` effect — a binary,
chance-based status with no magnitude, drawn from the sim RNG) and `resolute` (a creep
immune to `slow`, the counterplay `stun` answers) — plus a fifth Card in the Rail and wave
index 4's `resolute` × 6 / `fast` × 6 composition. Two genuinely new cues land: the **stun
telegraph** (a timed per-creep status, same essential-cue posture as the slowed/DoT
telegraphs before it) and the **ward** (a NEW kind of cue — persistent and catalog-derived,
not tied to a sim-state timer, because it marks "this creep carries an immunity" rather than
"this effect is currently active"). New and changed accessibility-relevant surfaces:

| Surface                                                                      | Status     | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ---------------------------------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **The fifth Card: keyboard + AT path**                                       | ✅         | `armTower5` (Digit5 by default) joins `armTower1`–`armTower4` — the same widened-not-redesigned path each prior Card followed, needing no keymap change (nine hotkey slots already existed generically, P6). Unit-tested: `overlay.test.ts`, `controller.test.ts`, `keymap.test.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Tower/creep names — open catalog, never a raw id**                         | ✅         | `TOWER_NAME` gains `stun` (`"Stun Tower"`), `CREEP_NAME` gains `resolute` (`"Resolute Creep"`) — the same partial literal-key map + localized `tower.unknown.name`/`creep.unknown.name` fallback strategy every prior story's row above covers. Asserted in the armed, placed, Card, and Panel paths (`overlay.test.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **Panel stun stat — chance + duration, both as TEXT**                        | ✅         | `appendStatRows` gains a `panel.stun` row (`"Stun: {chance}% for {duration}s"`), shown whenever the armed/selected tower has a `stun` effect and omitted entirely otherwise (`stats.stun === null`, mirroring `dot`'s null-omission posture). Without this row, stun's two defining numbers — the shipped tower's 25% chance and 1.0s (20-tick) duration — would be invisible to a player who cannot perceive the canvas telegraph. Unit-tested (`overlay.test.ts`, armed AND selected-tower paths).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Preview immunity text — the first NON-EMPTY immunities row**               | ✅         | Every creep before this story compiled with `immunities: []`, so the wave preview's `hud.preview.entry` text has always read "no immunities" for every entry, on every wave. `resolute`'s `['slow']` is the first entry in the game's history to exercise the non-empty branch (`IMMUNITY_NAME`, already covering both `slow` and `stun` since the immunity axis was plumbed ahead of this story) — verified it actually renders (`"...armor 0, slow"`), not merely that the plumbing compiles (`overlay.test.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **The stun telegraph — dual-encoded, canvas-only, ESSENTIAL not decorative** | ⚠️ PARTIAL | A stunned creep draws a shape cue that is ALWAYS present — a thick ring (`jolt`, r×1.15, weight 4) in `palette.stunned`, drawn OUTSIDE the silhouette so its contrast partner is the board floor — plus a motion cue: a second, SMALLER inner ring (`flicker`, r×0.85, weight 2) whose alpha oscillates on the same triangle-wave clock the slowed telegraph's pulse uses, that is OMITTED ENTIRELY under reduced motion (not merely damped — WCAG 2.3.3 / GAG §2, same posture as the slowed/DoT telegraphs' motion cue). The two rings sit at deliberately different radii (an alpha-animated ring at the same radius/colour/width as an opaque one beneath it would be invisible — the animation would have nothing to modulate), asserted directly (`creep-paint.test.ts`). `palette.stunned` is gated in `palette.test.ts`'s permanent contrast suite as an essential opaque cue at ≥3:1 against the board floor — and, uniquely among this checklist's telegraphs, ALSO at ≥3:1 against `creep`, belt-and-braces for the `flicker` cue that does still draw inside. The jolt was FIRST drawn inside the silhouette at r×0.55, where its partner was the creep fill; below `hpFrac` 0.34 that fill is `creepLowHp`, against which `stunned` measures 1.10:1 — and no colour can fix it (clearing ≥3:1 against the floor forces L ≥ 0.141 and against `creep` forces L ≤ 0.215, while `creepLowHp` demands L ≤ 0.041 or L ≥ 0.766; disjoint in the default and protan/deutan tables — tritan's `creep` is dark enough to open the band, so it inherits the geometry rather than needing it, see `palette.test.ts`). The GEOMETRY moved rather than the colour: an essential cue must not depend on the health of the thing it is drawn on. **Known cost, deferred with the rest of the cue-radius layout:** at small cell sizes (cellPx ≈ 29, e.g. 1000×700 on `field-01`) the jolt at r×1.15 and the slowed ring at r×1.4 merge into one band. Centred strokes need `jolt_r < 1.4 − 3/r`, a bound that tightens as cells shrink and reaches 0.4 at the `r = max(3, …)` clamp — below the 1.0 an outside ring must exceed — so no size-independent radius exists and the fix is the cue-radius layout pass, not a different number. `creep-paint.test.ts` pins the jolt strictly inside r×1.4 so the ordering cannot degrade further. There is deliberately NO per-application live-region announcement for a stun landing — the existing "HUD is not a live region" rationale extends here too. Colour is never the sole channel. The telegraph lives entirely on the Phaser canvas (axe-invisible by construction — ADR 0003 §3); its draw calls thread through `board-draw.ts`'s recording-double test (`scene.test.ts`) the same way the DoT telegraph's did. |
| **The ward — persistent, catalog-derived, deliberately NO motion cue**       | ⚠️ PARTIAL | A creep carrying any immunity (`(def?.immunities.length ?? 0) > 0`, catalog-joined like `hpFrac`, no sim state — CONTEXT.md's new **Ward** term) draws a single stroked ring at r×2.2 in `palette.warded`, OUTSIDE the slowed pulse's r×2.0 ceiling so the two never overlap ambiguously. Opaque (alpha 1) deliberately: `palette.test.ts`'s contrast gate checks the source colour, which only proves the RENDERED ratio when nothing composites over it. **This is a considered divergence from the slowed/DoT/stun telegraphs' dual-encoding pattern, not an oversight**: a ward is not a timed status (nothing to start, tick, or expire), so it carries no `renderTimeMs` parameter and no reduced-motion branch — persistent presence is the whole signal (Rob, 2026-08-04: "they should see _something_"). Reactive per-shrug feedback (an immune hit visibly bouncing off) was considered and deferred on cost, not blocked on determinism — recorded in PLAN.md and carried forward as a P9 doc note; this checklist records only what shipped. A forged/unresolved `creepId` falls back to `warded: false` rather than throwing — totality, matching the adjacent `hpFrac` join's own posture. Unit-tested (`creep-paint.test.ts`, `render.test.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **`stun` tower footprint mark — id-keyed, colour-redundant**                 | ✅         | `stun` shares `palette.tower`'s colour with every prior tower (this story mints no fifth tower colour); the distinction is a three-segment zigzag `'bolt'` mark, non-radial so it reads as distinct from `'crosshair'`'s spokes at a glance. The `TowerFootprintMark` vocabulary widens to add `'bolt'` rather than reusing any existing value, so no two towers share a footprint mark. Threads through the pending-build pipeline exactly like every prior mark. Unit-tested (`tower-paint.test.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **`resolute` creep silhouette — id-keyed shape**                             | ✅         | `resolute` draws a regular pentagon, visibly distinct from `normal`'s triangle, `fast`'s diamond, `swarm`'s square, AND `armored`'s hexagon at cell scale. A fifth shape cue in the same `creepShapeFor` total-over-any-string map (an unrecognized id still falls back to the triangle). Unit-tested (`creep-paint.test.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |

**No status-effect wording drift:** the table above uses the canonical glossary terms
**Telegraph** and **Ward** (this story's grill-with-docs session added the latter to
`docs/CONTEXT.md`) — held by review, same as every prior story's discipline.

**Two rows above are ⚠️ PARTIAL, and both reasons are deterministic, not device caveats.**
Both are residuals of the same missing work — a cue-radius layout pass over all four
telegraph families — so they carry the same status rather than being graded differently.

The **stun** row's original gap WAS fixed, and that part is not deferred: the guaranteed
`jolt` ring drew inside the silhouette at r×0.55, where its contrast partner was the creep
fill and it measured 1.10:1 against `creepLowHp`; it now draws outside at r×1.15, partnered
against the board floor at 3.87:1. What remains is a NEW residual introduced by that move —
at small cell sizes the jolt merges with the slowed ring at r×1.4 — and since no radius
fixes that (see the row), it belongs to the same deferred pass as `warded` and is marked
the same way. Grading it ✅ while it carries a deferred cost of exactly the class that makes
`warded` ⚠️ PARTIAL would be two criteria for one defect.

The **ward** row:
`palette.warded`'s r×2.2 ring spans ~1.54 cells, so it is routinely drawn over `tower` at
**2.37:1** in the default and tritan tables (protan/deutan clears it at 3.59). It is gated against `tower` by byte-DISTINCTNESS instead — the same posture
`poisoned`'s pips at r×1.8 already carry — so this is a consistency gap in the cue-radius layout
rather than a new hole, and it is owed to the art pass. A real-device capture would
not change the number.

**Real-device items still deferred to #36** (this story adds nothing new to verify there
beyond what Story 3/4/5/11 already deferred): the stun telegraph's jolt/flicker legibility
and the ward's ring legibility at the narrow floor are asserted only by the paint-plan unit
tests (`creep-paint.test.ts`) and by e2e's DOM/axe stability, not a real-device capture — the
same posture the slowed and DoT telegraphs both already carry. `scene.ts` itself stays
coverage-excluded by long-standing convention (not meaningfully testable under jsdom).

## Story 7 conformance audit — air: the domain axis, `antiair` and `flying` (M2-S7)

M2-S7 activates the **domain** axis: `flying` creeps, the `antiair` tower, `slow` going
both-domain, and wave index 5 (8 × `flying`). This backfills the section four later
stories flagged as missing (#104) — mostly transcription from PR #87's body, `palette.test.ts`,
`creep-paint.ts`, and `tower-paint.ts`, not fresh assessment. Two genuinely new
accessibility-relevant surfaces land — a tower footprint mark and a creep cue — plus a Panel
row that stops hardcoding a value. New and changed accessibility-relevant surfaces:

| Surface                                                                   | Status     | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ------------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **The sixth Card: keyboard + AT path**                                    | ✅         | `armTower6` (Digit6 by default) joins `armTower1`–`armTower5` — the same widened-not-redesigned path every prior Card followed. Unit-tested: `overlay.test.ts`'s "Digit6 arms the sixth Card with the six-tower bundle (M2-S7 P5)" asserts the Card's name, hotkey text, `aria-keyshortcuts`, and the dispatched action.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Tower/creep names — open catalog, never a raw id**                      | ✅         | `TOWER_NAME` gains `antiair` (`"Anti-Air Tower"`), `CREEP_NAME` gains `flying` (`"Flying Creep"`) — the same partial literal-key map + localized `tower.unknown.name`/`creep.unknown.name` fallback every prior story's row covers. `antiair`'s name is asserted directly in `overlay.test.ts`'s Card test above; `flying`'s renders through the wave preview's data-driven text and is pinned in e2e (`apps/web/e2e/smoke.spec.ts`'s `EXPECTED_COMPOSITION`, keyed by 1-based wave NUMBER — so this wave, index 5 above, is key `6` there, and its single-element value is `"8 × Flying Creep — air, armor 0, leak cost 1, no immunities"`) rather than by a dedicated overlay unit test — `flying` never gets a Card of its own, so there is no Card-name assertion for it the way every tower gets.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **The Targets row — domain-derived, no longer hardcoded**                 | ✅         | Before this story every tower's Panel Targets row rendered the hardcoded catalog key `tower.targets.ground`, correct only because sv7's capability profile had never compiled anything else. `overlay.ts`'s `targetsFor(domain)` reads the armed/selected tower's actual `attack.domain` and resolves one of three new/existing keys (`tower.targets.ground`/`.air`/`.both`); at S7 an unresolved `def` fell back to `'ground'`; M2-S8 replaced that with the omit-rather-than-lie rail the Panel uses now (`overlay.ts` returns `null` for an attackless tower and drops the row), so the fallback described here is S7-era, not current. Without this change `antiair` would have displayed the honestly-false "Targets: Ground". Unit-tested (`overlay.test.ts`: "the Panel's Targets row reads Air for antiair and Ground + Air for the both-domain slow tower").                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **`antiair` footprint mark — id-keyed, colour-redundant**                 | ✅         | `antiair` shares `palette.tower`'s colour with every prior tower; the distinction is `'arrow'` — a shaft through the footprint centre plus two barbs at the tip, distinct from `'crosshair'`'s four spokes and `'bolt'`'s zigzag (the marks are enumerated in `tower-paint.ts` and drawn in `board-draw.ts`). Deliberately NOT a bare chevron: the airborne cue below is exactly that glyph and lands in the cell to a flyer's north — usually a tower footprint, `antiair`'s own by definition when it is in range — so a chevron mark here would put two same-shaped cues a fraction of a cell apart with only colour separating them, which ADR 0003 forbids. Unit-tested (`tower-paint.test.ts`: "draws antiair with a distinct arrow mark (M2-S7)").                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **The airborne cue — a wing chevron, catalog-derived, no motion channel** | ⚠️ PARTIAL | `airborneCuePaintOps` (`creep-paint.ts`) draws two strokes fanning down from an apex above the silhouette to tips that clear it vertically — the tips sit at `x ± 0.9r`, INSIDE the silhouette's half-width, not beyond its sides, composed alongside whichever base shape `creepShapeFor` already draws — never a sixth mutually-exclusive `CreepShape` — which is why S10's `armored-flyer` can read as armored AND airborne with zero new render code. Like `wardPaintOps`, this is CATALOG-derived (`CreepVM.domain === 'air'`), not a timed status, so it carries no `renderTimeMs` and no reduced-motion branch — shape is the only channel, same posture as the ward. Gated in `palette.test.ts`'s permanent contrast suite both ways: `contrast(pal.airborne, pal.tower) ≥ MIN_CUE_CONTRAST` in all four colour modes (the cue is drawn a full cell above the creep, ordinarily over a tower footprint on the shipped board), and for pairwise distinctness against `spark`/`creep`/`creepLowHp`/`tower`/`floor`/`stunned`/`warded`/`slowed`/`poisoned`. PR #87 records the airborne-vs-tower figures as measured at ship time: 3.08:1 default, 4.67:1 protan/deutan; tritan is not separately cited in the PR body, and this audit does not assert a number for it beyond what the permanent per-colour-mode loop already guarantees ≥3:1. **Residual, not a new gap:** PR #87 also records tritan's `airborne` vs `stunned` at ~1.10:1 — below the contrast floor, and not separately gated by contrast (only by byte-distinctness, the same posture `warded`-vs-`tower` already carries in the Story 6 audit above). Shape carries the separation instead: the stun jolt is a ring at `r×1.15`, the airborne cue is a pair of strokes whose apex sits at `r×3.4`, tips ≈`r×3.23` out, clear of the stun jolt by a wide margin. **Closed by #126, and the wider claim now holds too.** When this row was written, `creep-paint.ts`'s cue-radius comment block listed jolt 1.15 / ring 1.40 / pulse 2.00 / ward 2.20 / airborne 2.60+ and OMITTED the DoT cues, and the outermost of those — the drift, centred at `r×2.40`, drawn extent `r×2.83` at the narrow floor — reached past the airborne apex, which sat at `r×2.9`. So "outside every timed cue" was NOT established; only "outside the stun jolt" was. #126 measured the overlap (−0.838px at the `CELL_PX_MIN_NARROW = 10` floor, between a drift pip's filled disc and the chevron's 2px strokes), moved the apex out to `r×3.4`, enumerated the DoT cues in the ordering block, and pinned the clearance in `creep-paint.test.ts` — so "outside every timed cue" is now measured rather than asserted. The remaining airborne residual is the adjacent-cell spill, which belongs to the same deferred cue-radius layout pass as the Story 6/8 partial rows. |

**No terminology drift:** the table above uses the canonical glossary terms **Domain**,
**Telegraph**, and **Ward** (not new to this story, but exercised by it for the first time
against a real second domain value). This story's own commit also corrected
`docs/CONTEXT.md`'s Domain entry (`ground/flying` → `ground/air`) and added the
disambiguation that an air creep is colloquially a _flyer_ but `flying` is a catalog creep
id, not a domain value.

**Real-device items deferred to #36** (nothing new beyond what Story 3/4/5/6 already
deferred): the `'arrow'` mark's legibility at the narrow floor and the airborne cue's
legibility where it spills into the neighbouring cell are asserted only by the paint-plan
unit tests (`tower-paint.test.ts`, `creep-paint.test.ts`) and by e2e's DOM/axe stability,
not a real-device capture. `scene.ts` itself stays coverage-excluded by long-standing
convention (not meaningfully testable under jsdom).

## Story 8 conformance audit — support: the `beacon` and its aura (M2-S8)

M2-S8 adds `beacon` — the first tower in the game's history that does **not attack**. It has
no range, no cadence, no target domain, and deals no damage; it raises the damage amounts of
the attacking towers whose 2×2 footprint shares a full cell edge with its own (CONTEXT.md's
new **Aura** term). That "no attack" is the accessibility story here as much as the aura is:
four Panel rows that every prior tower carried are now absent by construction, and two board
cues are new. New and changed accessibility-relevant surfaces:

| Surface                                                   | Status     | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| --------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **The seventh Card: keyboard + AT path**                  | ✅         | `armTower7` (Digit7 by default) joins `armTower1`–`armTower6` — the same widened-not-redesigned path every prior Card followed, needing no keymap change (nine hotkey slots already existed generically, P6). The absent-slot assertion narrows from 7–9 to 8–9 accordingly. Unit-tested (`overlay.test.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **Tower name — open catalog, never a raw id**             | ✅         | `TOWER_NAME` gains `beacon` (`"Beacon"`), the same partial literal-key map + localized `tower.unknown.name` fallback every prior story's row covers.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **Panel: four rows OMITTED, never shown as zeros**        | ✅         | A `beacon` shows no Damage, Range, Fire rate or Targets row at all — the omit-rather-than-lie posture `panel.blastRadius`/`panel.dot`/`panel.stun` already set, now applied to the base stats. Shown as `0`/`0 tiles`/`20/s`/`Ground` they would each be a false statement about a tower that has none of those properties, and a screen-reader user has no canvas to correct the record against. `TowerStats.damage`/`rangeTiles`/`fireRate`/`targets` are `string \| null`, so a future tower cannot re-introduce the zeros by accident. Unit-tested (`overlay.test.ts`, asserting the four row LABELS are absent from an armed beacon's Panel — an assertion on values would pass on a Panel printing "Damage: 0").                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **Panel: the Support row — the aura's magnitude as TEXT** | ✅         | `panel.support` (`"Support: +{percent}% damage to towers sharing a full edge"` — **not** "adjacent", which does not convey that corner-only touch is excluded) is the ONLY non-canvas carrier of what the beacon actually does, so it is the row that keeps the tower usable without the board. The percentage is **derived from the def's own `damageMulFp`**, not the literal "+50%" the shipped beacon happens to have — every other `towerStats` row is data-driven, and a hardcoded number would make the Panel lie for any modded bundle.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Panel: a buffed tower reads its REAL damage**           | ✅         | A tower inside an aura shows the damage it actually deals (15, not the catalog's 10), under a distinct `panel.damageBuffed` label so the raised number is never mistaken for the tower's own printed stat. The multiplier is read LIVE from the current board, not captured when the tower was selected, so building or selling a beacon beside a selected tower moves the row — and the rows are re-rendered IN PLACE for that case (`panelStatsEl`), never by re-keying the Panel, so a keyboard user with focus on Sell or Close keeps it while the Panel stays open on the same selection. Asserted in both respects (`overlay.test.ts`): the number changes, and `document.activeElement` does not. The multiplier reads COMMITTED sim state, deliberately unlike the selection hit-test and the Sell refund, which read the pending projection: those answer questions about the PLAN ("which tower is here", "what would selling return"), where a pending build belongs in the answer, whereas this row answers a question about the SIM ("what damage does this tower deal") — and a pending beacon deals nothing yet. Projecting it made the Panel the only one of three surfaces (Panel, board, sim) claiming a buff that was not yet real. Buffed per effect then summed — `buffAmount(Σ) ≠ Σ buffAmount(each)` under floor-rounding. **The Poison row is buffed too**, and it is the one this first shipped wrong: the sim buffs a `dot`'s per-tick amount at fire time (a `venom` beside a beacon really applies 6/tick, not 4), so a Panel that buffed only the Damage row misreported half that tower's output — on exactly the pairing this story showcases, in the surface that is a screen-reader user's only carrier of it. Cadence and duration stay raw, deliberately (m2.md: non-damage magnitudes and durations are never buffed). Unit-tested against a buffed AND an unbuffed control (`overlay.test.ts`), so the row cannot pass by always printing the buffed number.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **The aura shell — a rounded RECT, drawn on the floor**   | ⚠️ PARTIAL | A support tower strokes a rounded rect one cell out from its 2×2, bounding the cells a recipient must reach. **It over-approximates them, deliberately recorded rather than glossed:** the stamped aura is a plus-shaped ring of 8 cells (the four diagonal corners are excluded — corner-only touch never buffs), while the rect encloses those corners too, so a corner-only neighbour sits partly inside a boundary that does not reach it. The recipient ✦ is the authoritative signal — derived from the sim's own rule, drawn only on towers genuinely buffed — and the shell is the coarser "roughly here" cue. Drawing the true plus outline instead changes the aura's approved visual shape (Act 1 ruling 1), so it is raised for the product owner rather than taken in a review round. Deliberately not a circle: the rule is a square edge share (a tower diagonally OFFSET at `(c+2,r+1)` still qualifies while sitting outside an edge-fitted circle), and a second concentric circle on a footprint was already rejected as ambiguous in S4a's ship review — "is the inner one the splash, a permanent aura, or the range?" — naming this exact confusion. `palette.aura` (pale lavender) is gated in `palette.test.ts`'s permanent contrast suite **composited at the alpha it is actually stroked at** (`AURA_SHELL_ALPHA`, imported from the draw site so the gate cannot drift from the draw), clearing 7.64:1 against the floor in all three colour modes. Shape carries the meaning; colour is redundant. **Residual:** the shell is a canvas-only cue with no DOM equivalent — the Panel's Support row states the mechanic in text, but _which_ towers are currently in range is available on the board only. Same class of residual as every other canvas telegraph in this checklist, and it inherits the same deferral.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **The recipient ✦ — the aura read from the other end**    | ⚠️ PARTIAL | Each buffed tower draws a four-stroke star inside its footprint's top-left CELL, while every `TowerFootprintMark` is anchored at the footprint CENTRE — separated by position. Stated precisely, because an earlier draft of this row claimed the stronger "the two marks never merge" and the geometry does not support it: a `size * 0.22` mark reaches 0.56 × cell from the anchor against the ✦'s 0.68, and `'bolt'` (`stun`) is drawn at `size * 0.5` and spans the whole footprint deliberately, so no placement inside the body could avoid it. What is guaranteed and tested is containment (inside the body, inside the top-left cell); where the two abut at the narrow floor is a legibility residual, recorded below. It strokes `palette.floor` against the solid `palette.tower` fill — the same rule every footprint mark already follows, and the reason it is not `palette.aura`: near-white is the only band clearing 3:1 against `tower` in the default table (pale lavender measures 1.89:1 there), the same luminance sandwich the `stunned` row above documents. **Because it strokes the floor colour, anything drawn outside the body is invisible — so containment is a correctness property, not polish.** The first version sat at 0.42 × cell with r = 0.26 × cell, which at the narrow floor (`CELL_PX_MIN_NARROW` = 10, where the body is 16px across with a 6px corner radius) put its top and left tips at 1.6px against a body starting at 2px: most of the mark rendered floor-on-floor and what survived read as an asymmetric half-shape. A call-count test could not see it. A SECOND pair (0.5 / 0.15) then passed a centreline-only test and still clipped — the mark is stroked 2px wide, so each tip carries 1px beyond its centreline and it is the OUTER EDGE that must clear the corner arc. The shipped values are `SPARKLE_CENTRE_FRAC` / `SPARKLE_RADIUS_FRAC` (`board-draw.ts`), currently 0.55 and 0.13; they are named rather than quoted here on purpose, because this row is the durable record and a reader who "restored" a literal from it would reintroduce a defect the tests exist to catch. `scene.test.ts` asserts all eight tips inside the rounded body **computed against the real geometry at that floor, minus the stroke half-width**, the same posture `creep-paint.test.ts` already takes. **The mark is gated on the tower having an attack**, so a beacon standing beside a beacon never draws it — the sim enacts no chaining, and an ungated flag would have drawn a relationship that does not exist. Unit-tested in both directions (`render.test.ts` for the classification, `scene.test.ts` for the strokes). **Residual:** at the smallest supported cell size the ✦ and the footprint mark are two marks inside one 2×2; separated by position, but not verified on a real device. |
| **`beacon` footprint mark — id-keyed, colour-redundant**  | ✅         | `beacon` shares `palette.tower`'s colour with every prior tower (this story mints no seventh tower colour); the distinction is the `'pylon'` mark — an upright mast with a crossbar and a wider base bar, distinct from `'arrow'` (which converges to a point, with its widest feature at the tip rather than the base) and from `'bolt'` (straight, not staggered). The `TowerFootprintMark` vocabulary widens rather than reusing a value, and `tower-paint.test.ts` now asserts the **invariant** — all seven marks distinct — not just this story's pair.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **The armed-beacon ghost — no degenerate range ring**     | ✅         | `GhostVM.rangeFp` and `SelectionVM.rangeFp` are both `number \| null`, and both draw sites skip the ring when it is null. This is the path a player hits FIRST — arming the tower to place it — and before this change the ghost stroked a circle unconditionally for every valid ghost, which for an attackless tower would have previewed a zero-radius dot the player had to decode, colliding with the footprint mark. Nullable rather than `0` so the omission is a type-level fact rather than a convention. Unit-tested (`scene.test.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |

**No terminology drift:** the table above uses the canonical glossary terms **Aura**
(added to `docs/CONTEXT.md` by this story's grill-with-docs session) and **Telegraph** — an
aura is deliberately NOT called a telegraph, since it marks a property of the maze rather
than a timed status on a creep.

**Draw order is part of the contract.** Every shell is stroked in its own pass BEFORE any
tower body is filled. The shell's edge falls exactly on the boundary between an
edge-adjacent recipient's two footprint columns — down the middle of the tower it points at —
so when it was drawn inside the body loop, whether that segment survived depended on
placement order: the same board rendered two different ways depending on which tower was
built first. The two-pass order makes bodies always win, which also keeps `aura` off
`tower`, a pairing it does not clear 3:1 against. Asserted in both build orders
(`scene.test.ts`).

**Known residual — the aura shell can cross the entrance/exit glyph.** `aura` is gated
against the board FLOOR (7.64:1 composited). The entrance and exit are painted as their own
filled glyphs before towers, so a beacon placed at the board edge — legal on the shipped
28×24 board, e.g. anchored at (1,10), whose ring includes column 0 — puts about one cell of
the shell's perimeter stroke over them, where it measures ≈1.2:1. Recorded rather than gated
because it is the shape every canvas cue in this checklist already has (`poisoned`'s pips and
`warded`'s ring cross tower footprints on the same terms, gated by byte-distinctness rather
than contrast), the affected span is a fraction of a perimeter, and the ✦ carries the
authoritative per-tower answer regardless. The cue-radius/overlap layout pass this checklist
already owes is where this belongs, not a per-story colour change.

**Known gap, deliberately NOT closed here — an in-place Panel update is not announced.**
When a beacon is built or sold beside an already-selected tower, the stat rows re-render in
place (that is what keeps keyboard focus, above) but nothing is pushed to the `role="status"`
live region, so a screen-reader user with the Panel open hears nothing when Damage goes
10 → 15. This is the SAME posture this checklist already records for the stun telegraph —
"there is deliberately NO per-application live-region announcement for a stun landing" — and
for the HUD generally: a live region that fires on every sim-driven number change becomes
noise that masks the announcements which matter (placement outcomes, run transitions). The
value is not lost, only un-announced: it is present on the next Panel read. Deciding when a
derived stat change is worth interrupting a screen-reader user is a product judgement about
announcement budget, not a bug in this story, so it is raised rather than answered here.

**Known gap, deliberately NOT closed here — the aura is previewed only for COMMITTED
beacons.** Arming a beacon shows a bare footprint ghost, and a pending-add beacon
shows its outline and pylon mark but no shell, so the cells the aura will reach are not
shown until the build commits. Placement is the one decision that geometry governs, so this
is a real gap, and an attempt to close it inside this story's review loop is what surfaced
why it needs a design pass rather than a patch: the ghost is drawn AFTER the tower bodies,
so a ghost shell strokes `aura` over `tower` at 1.75:1 — under this repo's own 3:1 floor,
and the exact pairing the committed shell's two-pass order exists to prevent. Closing it
properly means previewing the aura consistently across the ghost, pending-add and committed
states, with a draw-order and colour decision for the over-tower case. Filed as follow-up;
the ghost's own obligation for THIS story — never previewing a degenerate range ring — is
met and tested.

**Known residual, deliberately not fixed here — a pending-sold beacon leaves its
recipients' ✦ standing.** `drawTowers` hides a committed tower whose sell is pending (the
"presented as already gone" rule of paused planning), but `TowerVM.buffed` is derived from
the COMMITTED tower SoA, which still holds the beacon until the tick commits — so while
paused, the shell vanishes and the ✦ remains. This is the pre-existing shape of the whole
pending-overlay mechanism rather than something this story opened: a pending-ADD tower
likewise does not block pathing, does not appear in `vm.towers`, and would not project an
aura either. Closing it means teaching the sim-derived view model about presentation-only
pending state, which is a larger change than this story should make and would put the
adjacency rule in two places — the one thing the design forbids. Recorded rather than
silently carried.

**Two rows are ⚠️ PARTIAL, and both are the same residual:** an aura is presented on the
canvas only. The Panel states the mechanic and the magnitude in text, so the tower is
purchasable and understandable without sight of the board; what is not available outside the
canvas is the live _adjacency_ — which particular towers are currently inside which aura.
That is the same shape as every canvas telegraph before it and is deferred with them, not
newly opened here.

**Gap superseded:** this row originally flagged that the checklist had no Story 7
conformance audit — S7 shipped `antiair`'s arrow mark and the airborne creep cue without
one. The section above (issue #104) backfills it; kept here rather than deleted so the
provenance of the four stories that flagged the gap stays legible.

**Real-device items still deferred to #36** (this story adds nothing new to verify there
beyond what Story 3/4/5/6/11 already deferred): the aura shell's legibility at the narrow
floor, and the ✦-beside-footprint-mark separation at the smallest supported cell size, are
asserted only by the paint-plan unit tests (`scene.test.ts`) and by e2e's DOM/axe stability,
not a real-device capture. `scene.ts` itself stays coverage-excluded by long-standing
convention (not meaningfully testable under jsdom).

## Story 9 conformance audit — burst: the `mine` (M2-S9)

M2-S9 adds `mine` — the eighth catalog tower, and the first in the game whose blast
**overreaches its own range ring**: `rangeFp` 576 (2.25 tiles) triggers the burst, but the
`aoe` effect's `radiusFp` 640 (2.5 tiles) is where the damage actually lands. Every prior
AoE tower (`splash`) has its blast sit INSIDE its range, so a selected `splash` showing
only the range ring never misrepresents anything; a selected `mine` doing the same would
show the player a smaller circle than the area the blast actually covers. Three new
accessibility-relevant surfaces land: a new footprint mark, a new board cue (the selection
blast spokes), and two new Panel rows. New and changed accessibility-relevant surfaces:

| Surface                                                                                                    | Status | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ---------------------------------------------------------------------------------------------------------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **The eighth Card: keyboard + AT path**                                                                    | ✅     | `armTower8` (Digit8 by default) joins `armTower1`–`armTower7` — the same widened-not-redesigned path every prior Card followed, needing no keymap change (nine hotkey slots already existed generically, P6). Unit-tested (`overlay.test.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Tower name — open catalog, never a raw id**                                                              | ✅     | `TOWER_NAME` gains `mine` (`"Mine"`), the same partial literal-key map + localized `tower.unknown.name` fallback every prior story's row covers.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **`mine` footprint mark — id-keyed, colour-redundant, and the vocabulary's first FILL**                    | ✅     | `mine` shares `palette.tower`'s colour with every prior tower (this story mints no eighth tower colour); the distinction is `'charge'` — a filled disc at the footprint centre, the same `size * 0.22` half-size the other centred marks use. It is deliberately the ONLY filled mark among eight, which is the real distinctness argument: at the `CELL_PX_MIN_NARROW = 10` compact floor `size * 0.22` is a ~4.4px RADIUS, so the disc spans ~8.8px, and at that size fine detail (a ring's thin stroke, a bolt's stagger) is gone, but "filled" vs "stroked" survives at any size — `'charge'` cannot be confused with `'ringed'`'s stroked circle of the identical radius even when every other shape cue has collapsed to a blur. Strokes/fills `palette.floor` against the solid `palette.tower` fill, the same channel every committed mark already clears — MEASURED (not assumed): 4.81:1 in default/tritan, 3.17:1 in protan/deutan, all four colour modes above the ADR 0003 ≥3:1 floor, computed from the existing `floor`/`tower` palette entries (unchanged by this story) using `palette.test.ts`'s own contrast function. `tower-paint.test.ts` asserts the widened 8-mark distinctness invariant, not just this story's addition. Unit-tested (`tower-paint.test.ts`, `scene.test.ts` for both the committed and pending-build draw paths, and that no OTHER shipped tower ever emits a `fillCircle`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **The selection blast spokes — a SHAPE cue, never colour alone**                                           | ✅     | A selected tower now draws the blast-radius crosshair (the identical `drawCrosshair` motif the ghost preview already uses at `scene.ts`) whenever `blastRadiusFp !== null` — the SAME condition the ghost uses, so arming a tower and selecting that same tower both answer "where does my blast land". Same condition and same motif, but NOT pixel-identical, and the difference is recorded rather than glossed: `drawCrosshair` leaves a centre gap of `spoke * 0.4`, which for the mine (blast 2.5 tiles) lands exactly on the body edge, but for `splash`/`frost-splash` (blast 1.5 tiles) leaves each spoke's inner ~22-44% over the solid `pal.tower` fill where `pal.range` is only 1.06:1. The ghost has no such band (its body is an outline, not a fill). The spoke TIP — what actually marks the radius — is on `floor` in both surfaces, so the cue is legible either way; a small-blast tower just shows a shorter visible spoke when committed. Residual, recorded here rather than fixed inside this story, since raising the gap would change how every AoE ghost draws too. `mine` is what forced the cue to exist: its blast (2.5 tiles) reaches PAST its own trigger ring (2.25 tiles), so a selected mine drawing only the ring would actively UNDERSTATE its blast — the cue that is supposed to answer "what does selecting this tower show me" would be misleading, not merely incomplete. This story first shipped a narrower gate (`blastRadiusFp > rangeFp`, satisfied only by `mine`) to leave `splash` untouched; that made arming a `splash` preview spokes while selecting it showed none, and it was re-ruled for consistency (Rob, 2026-08-07) — so `splash` and S10's `frost-splash` DO gain a selection cue they did not previously draw. Shape-distinct from the smooth range circle by construction (the Codex R1-15 ruling against a second concentric circle, invoked again rather than reopened) — never colour alone. `scene.test.ts`'s pre-existing "a selected tower draws the range-ring strokeCircle" assertion (exactly one `strokeCircle`) is UNCHANGED and stays green: `drawCrosshair` draws `lineBetween` calls, not circles, so the spokes add to the `lineBetween` count, never the `strokeCircle` one. Unit-tested in three directions (`scene.test.ts`): a mine-shaped selection, a splash-shaped selection (both draw spokes — the consistency the ruling bought), and the `blastRadiusFp === null` case, which is now the only negative control the gate has. |
| **Panel: the range row reads as a TRIGGER, not a firing range**                                            | ✅     | A burst tower's range row now emits `panel.triggerRange` ("Trigger range: {tiles} tiles") instead of `panel.range` — a mine does not shoot at what enters its ring, it detonates, and `panel.range`'s implicit "fires within" framing would misdescribe that. Gated on one new `TowerStats.attackMode: 'cadenced' \| 'burst' \| null` field, not two independent booleans, so the range label and the single-use row below (next row) can never disagree about what kind of tower is on screen — they read the same field. `null` for a support tower (`beacon`), the same omit-rather-than-lie posture every attack-derived row already takes. Unit-tested (`overlay.test.ts`, an armed mine's Panel AND a cadenced tower's unchanged Panel).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Panel: the single-use row — the one fact nothing else conveys**                                          | ✅     | `panel.singleUse` ("Destroyed when it fires") appears only when `attackMode === 'burst'`. This is the single most important fact about a mine that no other surface states: the board draws no "used up" cue (a fired mine's row is simply deleted from the sim at the fire tick, per P1–P4), and every OTHER Panel row describes what the tower does while it still exists. A screen-reader user with no canvas would otherwise have no way to learn that a 6-bounty purchase is consumed the first time it fires. Unit-tested alongside the trigger-range row, and asserted ABSENT on a cadenced tower's Panel so the field is proven to gate on data, not to fire unconditionally.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Panel: the Blast radius row — kept, and now the ONLY text carrier that the blast reaches past the ring** | ✅     | `panel.blastRadius` already existed (`splash`, M2-S4a) and needed no new code — `burst/aoe` compiles to the same `kind: 'aoe'` effect `blastRadiusTiles` already reads. What is new is what the row now HAS to do: for `mine`, it is the only TEXT statement that the blast (2.5 tiles) exceeds the trigger range printed right above it — authored as 2.25 tiles (`rangeFp` 576) and rendered `2.3` by the Panel's shared one-decimal `formatNumber`, the mine being the first tower whose range does not land on a tenth; the ORDER is what the row has to convey and that survives the rounding — the board spoke cue is the shape channel, this is the guaranteed non-canvas one, and a screen-reader user reading both numbers in sequence gets the full asymmetry without sight of the board at all. Unit-tested (`overlay.test.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Live region: `live.destroyed` — the only announcement the player did not cause**                         | ✅     | A detonating `mine` deletes its own row mid-tick, so the Panel closes and the range ring vanishes with NO player action behind it. Every other `PlacementOutcome` this live region carries (`armed`/`disarmed`/`placed`/`rejected`/`sold`) answers something the player just did and already knows about; this one is the reverse, and for a screen-reader user it is the ONLY signal that anything happened at all — the board's own cue for it is "a tower is no longer drawn", which is not a cue without sight of the board. New `PlacementOutcome` variant `{ kind: 'destroyed' }` (`controller.ts`), new key `live.destroyed` ("The selected tower was destroyed."), announced through the same polite region and the same `outcomeSeq` identity every sibling outcome uses — no new region, no new mechanism. Deliberately distinct from `sold`: a sell already announced itself at queue time, and the tick-time reconciliation branch is unreachable for sells (`sellSelected` clears the selection synchronously), so this branch can only ever be a destruction. Unit-tested on both sides (`controller.test.ts`: a real mine placed, selected, and advanced until it detonates, asserting `lastOutcome` is `{kind:'destroyed'}` and that a SELL still reports `sold`; `overlay.test.ts`: the rendered string, alongside every sibling outcome's).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |

**No status-effect wording drift:** `mine` introduces no new glossary term — a burst tower's
firing discipline (`CompiledTower.attack.mode`) was already named and defined by packets
P1–P4; this story is presentation over an existing domain concept, not a new one.

**The dock/rail now carries eight towers — observed, not redesigned.** Both layouts already
handle a variable-length tower list by SCROLLING rather than reflowing: the Standard `.wy-rail`
is `overflow-y: auto` inside its own grid track (`ui.css`), and the Compact `.wy-dock` gets the
same `overflow-y: auto` with a `max-height: 70%` cap in the `@media (max-height: 500px)` fork.
Neither layout has a fixed card count baked into its CSS, so the eighth Card does not break
either one structurally — inspected against the stylesheet, not captured on a real device or
driven through Playwright (out of this packet's verification list). What DOES visibly cost
more at eight towers than at seven: reaching the eighth Card now costs one more scroll step
in the Rail on a short viewport, and Compact's Dock — sharing its column with the HUD chips
above it via `flex: 1 1 auto` — has proportionally less room per remaining control as the
list above it grows. Neither is a new failure mode this story introduces; both are the same
scrolling degradation every prior tower already contributed to, one card closer to wherever
it stops being tolerable. **S12 owns the redesign at nine towers** (PLAN.md); this audit
records that eight is visibly closer to that edge, not that it has been crossed.

**Gap superseded:** this row originally re-flagged that Story 7 still had no conformance
audit, two stories after S8 first noted it. The section above (issue #104) backfills it;
kept here rather than deleted so the provenance of the four stories that flagged the gap
stays legible.

**Real-device items still deferred to #36** (this story adds nothing new to verify there
beyond what Story 3/4/5/6/8/11 already deferred): the `'charge'` mark's legibility at the
narrow floor and the blast-spoke/range-ring pairing's legibility at small cell sizes are
asserted only by the paint-plan unit tests (`scene.test.ts`, `tower-paint.test.ts`) and by
e2e's DOM/axe stability, not a real-device capture. `scene.ts` itself stays
coverage-excluded by long-standing convention (not meaningfully testable under jsdom).

## Story 10 conformance audit — the finale content: `boss`, `armored-flyer`, `frost-splash` (M2-S10)

M2-S10 lands the last two creeps and the last tower, plus the story's one sim rule (a leak
can cost more than one life, and lives can overshoot below zero). No new cue channel is
added anywhere in this story — every surface below reuses an existing shape, ring, or text
slot, which is itself the accessibility argument: the concentric cue-ring budget is already
exhausted (silhouette / flicker / jolt / slow / DoT / ward / airborne apex), so `boss` is
scaled rather than given a new ring, and `armored-flyer` composes two existing cues rather
than minting a third. New and changed accessibility-relevant surfaces:

| Surface                                                                                          | Status | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ------------------------------------------------------------------------------------------------ | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **`boss` — a SIZE-only cue, no new shape, no new ring**                                          | ✅     | `boss` maps to the existing `'hexagon'` silhouette (`creep-paint.ts`) — the same shape `armored`/`armored-flyer` already use — and gains a role-driven size multiplier, `BOSS_SCALE = 1.5` (`board-draw.ts`), applied AFTER the existing `Math.max(3, …)` floor rather than inside it, so the boss is legibly larger even at the smallest cell size rather than clamped back to the same 3px floor as every other creep. `boss` is also stun-immune, so it carries the existing **ward** cue alongside its size — the two channels (size, ward) are what separate it from a plain `armored`, which shares its hexagon but has neither. No new `CreepShape` value, no eighth concentric ring. Unit-tested (`scene.test.ts`'s render-distinctness assertion; a `BOSS_SCALE` mutation to 1 was applied and observed to turn exactly that test red).                                                                                  |
| **`armored-flyer` — reuses `'hexagon'` plus the existing airborne cue, the composability proof** | ✅     | `armored-flyer` is armor + air composed from two creeps that already shipped separately (`armored`'s hexagon, `flying`'s airborne apex cue) with zero new render code beyond a two-row `CREEP_SHAPES` addition. This is the file's own documented composability argument (`creep-paint.ts`), now exercised rather than merely asserted. The airborne cue's known spill past its own cell (recorded pre-S10) is unchanged — `armored-flyer` inherits it, it does not worsen it.                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **`frost-splash`'s `'ringed-crosshair'` footprint mark, in BOTH committed and pending states**   | ✅     | A genuinely new mark value — never a reuse, per the file's own rule — composed of the existing `'ringed'` circle plus four short outward-radiating spokes, so it reads as its two parents (m2.md) while staying shape-distinct from `'ringed'` (no spokes), `'crosshair'` (centre-gapped spokes, no ring) and `'charge'` (filled, not stroked). Drawn through a shared `drawRingedCrosshair` helper called from BOTH of `board-draw.ts`'s mark-draw branches — the committed branch (`pal.floor` stroke, weight 2, against the solid tower fill) and the pending branch (`pal.tower` stroke, weight 1, alpha 0.6, an unfilled outline) — which differ on purpose (CodeRabbit #73) and are tested separately rather than through one shared call site. A targeted mutation (deleting the arm from the pending branch only) is a named mutation-kill in the plan specifically to prove the two-branch trap is covered, not assumed. |
| **Wave preview — leak cost as an always-present accessible slot**                                | ✅     | The preview's per-entry text gains a sixth stat slot, `hud.preview.leakCost` ("leak cost {leakCost}"), following the `hud.preview.immunities.none` precedent: the slot is always present and names the common case (`leakCost` 1) rather than being hidden until a boss makes it interesting. `m2.md`'s Wave preview contract line is amended and dated to record the sixth field. Unit-tested (`overlay.test.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Panel: the slow row — a pre-existing gap, closed as a side effect**                            | ✅     | `TowerStats` gains `slow`, joining `dotEffect`/`stunEffect` as a magnitude/duration row, derived from `mulFp` (never hardcoded, never buff-aware — matching `panel.stun` and m2.md's "non-damage magnitudes and durations are never buffed"). Without it `frost-splash` would describe itself as strictly worse and dearer than `splash` with the thing it is actually paying for invisible to any non-visual player. Fixes `slow`'s OWN Panel gap too — a screen-reader user could not previously learn a `slow` tower's magnitude or duration at all. A mutation removing the row is a named mutation-kill in the plan. Unit-tested (`overlay.test.ts`).                                                                                                                                                                                                                                                                        |
| **HUD lives — the true value, negatives included, deliberately unclamped**                       | ✅     | `hud.lives` ("Lives: {count}") already interpolates the raw `SimState.lives` value with no floor, so a boss leak that drives lives to −1 (or lower, on the same-tick aggregate path) is exactly what a screen-reader user hears and what the sighted chip shows (`♥ -1`). This is a decision to change nothing — ruling 4 — but a ruling with no test is only a preference: `overlay.test.ts` pins both `Lives: -1` and `♥ -1` alongside the pre-existing `Lives: 10` case, so a later clamp or format regression is caught rather than silently making the overshoot invisible on the one surface a player actually watches.                                                                                                                                                                                                                                                                                                     |

**Hand-checked against `docs/CONTEXT.md`'s `_Avoid_` lists** (issue #91 is now closed and the linter DOES scan this file, so this hand-check is
belt-and-braces rather than the only guard — the same gap S9's Codex P1 slipped through): this section
uses **creep** (never `enemy`/`mob`), **tower** (never `turret`), **lives** (never
`health`/`HP`), **leak cost** (never `leak penalty`), **ward** (never `shield`/`badge`),
and makes no bounty claims that would need the canonical word. No occurrence of
`indicator` for a telegraph either.

**Gap superseded:** this row originally re-flagged that Story 7 — and only Story 7 — still
had no conformance audit, the gap having persisted through S8, S9, and S10 by this point.
The section above (issue #104) backfills it; kept here rather than deleted so the
provenance of the four stories that flagged the gap stays legible.

**Real-device items still deferred to #36** (nothing new beyond what Story 3/4/5/6/8/9/11
already deferred): the boss's scaled size at the narrow cell floor and the
`'ringed-crosshair'`'s spoke legibility against `'ringed'` at small cell sizes are asserted
only by the paint-plan unit tests, not a real-device capture.

**The boss is the concentric stack's new worst case, and it is recorded rather than
resolved.** `BOSS_SCALE` multiplies `r` after the floor, which enlarges the WHOLE cue
stack rather than adding a ring to it — that is exactly why a size-only cue was available.
But the boss is stun-immune and therefore warded, so its ward ring now sits at
2.2 × 1.5 = **3.3 r**, which at ship time was past the airborne apex at 2.9 r that
`creep-paint.ts` already documents as spilling into the neighbouring cell. So S10 does not
introduce the spill, but it did move the worst case. **Corrected by #126**, which both moved
the airborne apex out to 3.4 r and enumerated the DoT cues the earlier ordering omitted — and
enumerating them shows the boss's ward was never the furthest-reaching cue in the first place.
Ranked in BASE r (`BOSS_SCALE` = 1.5 multiplies `r` before every multiplier below):

| cue                            | reach in base r                                               | reachable?                                                                                                                                |
| ------------------------------ | ------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| boss ward                      | 2.2 × 1.5 = **3.3**                                           | yes — the boss is stun-immune, so it is warded                                                                                            |
| airborne apex (ordinary flyer) | **3.4**                                                       | yes — any `flying`/`armored-flyer` creep                                                                                                  |
| **poisoned boss, drift crest** | 2.4 × 1.5 = **3.6** centre, **≈3.87 drawn** at ordinary cells | yes — the boss is ground-domain with `immunities: ["stun"]` only, and `venom` is ground-domain, so an ordinary endgame loadout poisons it |

So **the poisoned boss's drift is the furthest-reaching cue the renderer draws**, ahead of the
airborne apex, with the boss's ward third. (Note the asymmetry with the airborne/drift pair that
issue #126 fixed: a poisoned FLYER is unreachable in shipped content — every DoT source is
ground-domain — while a poisoned BOSS is an ordinary occurrence.) The spill's character is
unchanged throughout (still the one neighbouring cell); only which cue reaches furthest moved.
No cue is lost or occluded by it (the stack is concentric and stroked, so an
enlarged ring overlaps a neighbour's cell without hiding anything in its own), which is
why this is deferred to #36 with the other real-device items rather than treated as a
defect. The next story to add a cue radius, or to widen a creep's scale again, is the one
that has to look at the budget properly.

## Story 11 conformance audit — the showcase run: the four-row wave preview (M2-S11)

M2-S11 completes the ten-wave arc (see `docs/milestones/m2.md` item 11), and with it lands
the arc's own densest tick — arc wave 9 (wave index 8), four concurrent creep streams —
which is the wave preview's own worst case for row count. No new cue channel, no new
Panel row, and no new catalog surface land in this story; it is content plus verification
only. One new accessibility-relevant surface: the preview genuinely rendering four rows
at once for the first time (the prior maximum, from M2-S6's `resolute`+`fast`, was two).

| Surface                                                                          | Status | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| -------------------------------------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **The four-row preview at 658×320 — the bounded scrollport's worst case so far** | ✅     | Measured at the Galaxy-S9+-landscape floor (658×320, the same viewport every Compact audit in this file uses): the preview's existing bounded, keyboard-scrollable box (`.wy-hud`, contract §1) holds all four rows with **no clipping** — the preview's own right edge and every row's right edge stay inside the status column's right edge; **no font-size degradation** — the preview carries no font-size rule of its own and reads at `.wy-hud`'s base size at 1 row exactly as at 4; and **every row keyboard-reachable** via the same focus-and-scroll path the 1-row, 200%-zoom case already proved, now exercised against 4 real rows at 100% zoom rather than a synthetic overflow. An axe audit with all four rows visible reports **zero violations**. |
| **New e2e regression** (`apps/web/e2e/compact.spec.ts`)                          | ✅     | Drives real early-calls through arc waves 1–8 to bring wave 9's four-stream composition into the preview slot, then asserts row count, row text (name/count/domain/armor/leak cost/immunities per entry — the full six-field accessible text `m2.md`'s Wave preview contract specifies), the axe result, the no-clipping bound, the font-size equality, and keyboard reachability for all four rows, at the real compiled composition rather than a stand-in fixture.                                                                                                                                                                                                                                                                                               |

**Hand-checked against `docs/CONTEXT.md`'s `_Avoid_` lists**, following S10's own
convention (issue #91 is now closed — the linter scans this file, so this is belt-and-braces): this section uses
**creep** (never `enemy`/`mob`), **wave** (never `round`), **arc** (never `round list`),
**leak cost** (never `leak penalty`), and makes no `health`/`HP` claims about a tower or
the player.

**Gap superseded:** this row originally re-flagged that Story 7 — and only Story 7 — still
had no conformance audit, four stories having shipped and flagged it since S7 itself by
this point. The section above (issue #104) backfills it; kept here rather than deleted so
the provenance of the four stories that flagged the gap stays legible.

**#36 (accessibility real-device pass) cannot close at S12 (ruling 6, Rob, 2026-08-08,
re-accepted at S11 close-out 2026-08-09).** #36's real-device evidence spans every
story from S3 on, all still deferred to emulation; the pinned Chrome emulation profiles
remain the reference device through M2's close-out, and the low-end Android pass named
in ADR 0005 does not run in M2. Recorded here per this file's own convention of tracking
#36 alongside each story's real-device deferrals; see `docs/milestones/m2.md`'s Open
questions and ADR 0005's 2026-08-09 amendment for the full ruling.

**Real-device items still deferred to #36** (nothing new beyond what Story
3/4/5/6/8/9/10/11 already deferred): the four-row preview's legibility at the narrow cell
floor and under real device text-scaling is asserted only by the e2e/axe evidence above,
not a real-device capture.

## Playtest polish round (post-S11, 2026-08-10)

Two owner playtest reports reshaped the Standard chrome; the conformance-relevant facts:

| Item                                                | State                | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --------------------------------------------------- | -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Wave preview floats over the Stage on Standard      | ✅                   | The preview remains one DOM node and one AT surface in every home (`shell.placePreview` reparents; `stage-stability.spec.ts` pins single-node + re-homing across the Compact fork and the width bucket). Reading order on Standard now places it after the board (`role="application"`); it stays adjacent to the chips in Compact. In its RESTING form it is `pointer-events: none` — display-only, never a pointer target or tab stop. Its transient scroll form (content exceeding the px-capped clamp, at any zoom) takes pointer + a labelled tab stop (`role="group"`, `preview.label` — the `.wy-hud` scrollport discipline) to keep the composition reachable, over cells that were already visually occluded, and returns both the moment content fits — never a re-home, which would re-project the board from the content-sized status row (Codex #96 P1: the earlier ≥150% zoom fallback did exactly that and was deleted; the float serves every zoom level). The ONLY hud homes are static viewport properties — Compact, and sub-400px stages (portrait phones are Standard) — and there ui.css's row reservation pins the hud at its cap, so wave changes cannot move the board from that home either (`stage-stability.spec.ts` pins both). It is a declared layout region: the relations gate pins its grid clip to at most 40% of the grid's AREA, confined to the grid's left half. |
| Preview over board cells                            | ⚠️ recorded residual | The letterbox margin never hosts the ~256px card whole at ANY Standard viewport (measured margins: 46px at 1280×900 up to 110px at 1512×854), so an opaque top-left overlap of roughly 7 columns × 3 rows of board cells is the NORMAL Standard state — click-through in the RESTING form, off the entrance lane, bounded by the relations gate (at most 40% of the grid's area, confined to its left half), but a tower built beneath it is visually hidden while the preview shows; the transient scroll form takes pointer over that region as well, and there the card is input CHROME (`input.ts`'s `CHROME_SELECTOR`, Codex #96 P2) — a captured drag release over it cancels rather than silently placing on the hidden cell (keyboard placement unaffected — see the row above). Sighted impact only in the resting form (the cells stay fully operable there); revisit if playtests build there.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Status row stability                                | ✅                   | The row is the chips line alone (44px at 1280×720) — the board no longer re-projects mid-run on preview changes (WCAG 2.2 "Focus Not Obscured"-adjacent stability concern raised by the owner; `stage-stability.spec.ts` pins the invariant).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Card glyph swatches                                 | ✅                   | `aria-hidden` canvas per Card — a redundant visual channel repeating the board's footprint mark (ADR 0003: shape never colour-only; the mark itself is the distinction). The Card's accessible name is unchanged (text subtree). Repainted on colour-mode change (`main.ts`), so colour-vision modes stay coherent. Hidden on Compact (space, not capability — same ruling as the hotkey badge).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Two-column Rail (Standard, ≥1280w + aspect ≥ 16/10) | ✅                   | Same DOM, same tab order (catalog order, row-major visual = DOM order); every Card keeps its ≥44px target. All nine Cards + the open Panel fit without scrolling (`arming.spec.ts` at 1366×768); narrower Standard keeps the single-column scrollport with the S11 keyboard-reachability evidence unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |

## Story 12a conformance audit — the Compact build loop (M2-S12a)

M2-S12a is render/input and strings only (see `docs/milestones/m2.md` item 12). It adds no
cue channel and no catalog surface. It changes three accessibility-relevant things: the
Panel pins to the Rail's bottom edge where it could not otherwise be seen, its stat rows
take a condensed VISIBLE form on a narrow Rail, and the wave preview's armor clause now
states which damage armor applies to.

| Surface                                                       | Status | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------------------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **The armed Panel is on screen at all** (the defect)          | ✅     | Measured before the story at DPR 1: arming any tower at 658×320, 568×320, 900×480 or 640×560 rendered the Panel at content offset 677.5px with **zero pixels visible**. Now 182px, 182px, 268px and 268px at 100% text zoom, and 173px, 173px, 269px and 206px at 200%. Asserted per viewport per zoom in `compact.spec.ts`, arming by **hotkey** from `scrollTop === 0` — a Playwright `.click()` on a below-fold Card auto-scrolls the Rail and would have let every one of these pass with the defect fully intact.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **Focus is never obscured by the pinned Panel** (WCAG 2.4.11) | ✅     | A sticky Panel paints over earlier siblings, and native focus scrolling only guarantees a focused Card INTERSECTS the scrollport — not that it clears an overlay. The Rail reserves the Panel's CAP via `scroll-padding-bottom` while a pinned Panel is open, as a synchronous class toggle rather than an observer, so there is no moment at which a focused Card is covered. Tested by tabbing all nine Cards and asserting each focused Card's rect clears the Panel's top edge — and, separately, that the scroll fade's band never covers one either, in both the pinned and closed states (its gradient ends fully opaque at exactly the edge focus scrolling parks a Card against; before the band was reserved it erased 24px of a 58.4px Card, outline included). **Bounded at 100% text zoom, and the bound is stated because it is not a choice:** at 200% a Card is 225.8px tall while only 131.2px of Rail exists above the pinned Panel at 658×320, so full clearance is geometrically impossible there and the worst focused Card is 31% covered (19% at 640×560; 0% at 900×480). WCAG 2.4.11 (AA) is still met — the focused Card is partially visible, never entirely hidden — and the alternative would be a Panel that cannot show a tower. Recorded rather than smoothed over.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **The capped Panel is keyboard-operable** (WCAG 2.1.1)        | ✅     | The cap introduces an internal scroll container, and the stat rows are `<p>` elements that cannot take focus — so left alone it would have been operable by mouse and test only, a regression of this story's own making. The scrollport takes `tabindex="0"`, `role="group"` (never `region`: a landmark for a box whose only job is to scroll would announce itself in every landmark list) and a localized accessible name, granted **only when it actually overflows**. Proved by real key presses from that tab stop reaching every row — never `scrollIntoViewIfNeeded()`, which would prove only that the row exists. The test alternates `ArrowDown` and `PageDown`, and that is deliberate rather than belt-and-braces: in WebKit an active `requestAnimationFrame` loop suppresses arrow-key scrolling of a focused scroll container, and this app runs a continuous render loop — so an `ArrowDown`-only assertion would state a property that holds in Chromium and fails on the only engine iOS permits. Verified passing under WebKit by temporarily widening that project's `testMatch` — a MANUAL check, not a gated one: `playwright.config.ts` runs `compact.spec.ts` under the two Chromium projects only, so CI cannot see a WebKit regression here. The gap is recorded rather than claimed away, and the WebKit arrow-key suppression itself is logged for follow-up; `PageDown`/`Space`/`End` reach every row there today. The **Close button** is the one thing outside that scrollport and so can never be scrolled out of reach; the heading, the rows and the action row are all inside it, because a cap can only squeeze a box that is allowed to shrink and neither a `<p>` of wrapped text nor a 44px button is — chrome left outside the scrollport does not get squeezed by the cap, it overflows it into the void below the Rail's bottom edge (measured at 658×320 with a tower selected, before this split: 220px of content in a 182px cap, zero stat rows rendered and 17 of the Close button's 44px on screen). |
| **The condensed rows lose nothing for assistive tech**        | ✅     | #130's shipped dual-form split, reused: the full sentences remain the Panel's accessible text at **every** width and only the presentation forks. The glance block carries `aria-hidden="true"` permanently, so a screen reader is never offered two readings of one fact. Asserted in both directions — the glance visible with the full block sr-only at a 144px Rail, and the glance `display: none` with the full block in normal flow at 1000×720.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **The pinned Panel's top edge** (ADR 0003, non-text ≥ 3:1)    | ✅     | `.wy-panel`'s ordinary `#33465f` border is 1.63:1 on `--wy-surface` — adequate as a resting seam between two boxes on one surface, under the bar the moment that seam becomes the boundary between a floating overlay and the content beneath it. New `--wy-panel-edge` token at **3.42:1**, gated numerically in `ui-contrast.test.ts` because axe cannot evaluate a rendered boundary.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **The Rail's scroll fade**                                    | ✅     | **Decorative only, and this row says so rather than claiming it as an accessibility fix.** The Cards below the fold were already reachable by keyboard and AT — they are native buttons in tab order and focusing one scrolls it in. The fade exists for the sighted pointer user, who had no cue at all: a 0px scrollbar gutter and five of nine Cards below the fold. It is `pointer-events: none`, carries no text, and is never the sole carrier of anything.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **The armor clause (#132)**                                   | ✅     | The visible glance reads `armor −8 direct` — sign and scope, where it previously read `armor 8` and said neither. The accessible full form states the rule in a sentence. An earlier draft applied the reduction to "every hit", which would have told the player their poison was being cut too — the exact inverse of the mechanic, since damage over time bypasses armor entirely. Verified against the sim, not assumed. The clause is carried **only** by rows whose armor actually mitigates: `armor 0` keeps its bare form, because teaching a subtraction rule on a row that subtracts nothing is noise.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **axe**                                                       | ✅     | Zero violations at both Compact floors (658×320 and 568×320), with the pinned Panel both **open and closed** — the open state is the one this story creates.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |

**Hand-checked against `docs/CONTEXT.md`'s `_Avoid_` lists** (the linter scans this file —
belt-and-braces): this section uses **Rail**, **Card**, **Panel** and **Dock** in the senses
the new Interface section defines, **creep** (never `enemy`/`mob`), **wave** (never `round`),
and **Compact**/**Standard** for the two interface forms.

**Accepted cost, recorded rather than smoothed over.** While a Panel is pinned open at
658×320, the Cards browsable above it drop from 4 to **2** — measured on screen, and worth
noting because the owner accepted this trade on an estimate of 3. Reachability was judged
to beat browsing: every Card stays reachable by hotkey and by tab at all times, at-rest Card
visibility is unchanged at 4 of 9, and the Panel closes with Escape or its Close button.

**Real-device items still deferred to #36** (nothing new beyond what Stories 3–11 already
deferred): the condensed rows' legibility at 0.875rem on a real phone, and the pinned Panel's
edge contrast under real device colour profiles, are asserted by the computed-value and e2e
evidence above rather than a real-device capture. **#36 close-out and the top-dock header are
S12b**, which is device-gated — see #143.

**WAIVER for those deferrals, named rather than implied.** Owner **Rob**; ruling 6 of
**2026-08-08**, re-accepted at the S11 close-out on **2026-08-09** (recorded in full in the
Story 11 section above and in `docs/milestones/m2.md`'s Open questions). **Accepted scope:**
real-device capture for every story from S3 on stays deferred to #36 for the remainder of M2 —
the pinned Chrome emulation profiles are the reference device through M2's close-out, and
ADR 0005's low-end Android pass does not run in M2. Nothing in Story 12a widens that scope: its
two deferred items are the same two classes (legibility at a real DPI, colour under a real
display profile) the ruling already covers. This paragraph exists because the deferral was
previously carried by cross-reference alone, which is not the same as an identified waiver.

**Two further items accepted by the owner on 2026-08-18, in scope for this story:** the
`armor −{n} direct` wording stands as ruled (the glossary's **Armor** entry was sharpened
instead), and the pin's remaining blind range on ordinary desktop windows ships as-is and is
tracked in #145 rather than closed here — it is not a regression, and closing it would overturn
`arming.spec.ts`'s documented reveal-scroll control.
