# M2 — Breadth (milestone spec)

- **Status:** Spec ratified — implementation not started
- **Date:** 2026-07-26
- **Relates to:** [roadmap](../roadmap.md) · [Core Gameplay PRD 0001](../prd/0001-core-gameplay.md) ·
  [glossary](../CONTEXT.md) · [M1 spec](m1.md) · ADRs [0001](../adr/0001-monorepo-and-stack.md),
  [0002](../adr/0002-asset-and-content-licensing.md), [0003](../adr/0003-accessibility-standard.md),
  [0004](../adr/0004-localization-and-i18n.md), [0005](../adr/0005-performance-budgets.md),
  [0006](../adr/0006-input-command-and-replay-schema.md), [0007](../adr/0007-board-and-content-data-format.md) ·
  issue [#22](https://github.com/wynding/wynding/issues/22)

## What M2 is

M2 is the **breadth** milestone (roadmap; PRD 0001's activation row): the sim's combat vocabulary
completes and the catalogs open up — **more tower types, more creep types (including air +
anti-air), more waves** — with the **wave-clear and early-call bonuses active** and the
**early-call scoring input live**, all on **M1's single-path board**. It is a public **alpha**
under the full accessibility gate (ADR 0003) and externalized strings (ADR 0004), like every
milestone.

PRD 0001 fixed the shapes; this spec fills in M2's **numbers and remaining semantics** so there is
**nothing to guess during implementation**. Every quantity is a **first-pass value, chosen to be
tuned** — content changes bump `rulesetHash`, never code. Terminology is the glossary's
([CONTEXT.md](../CONTEXT.md)); this milestone's grill added four canonical terms there (**Effect
primitive**, **Wave preview**, **Boss**, **Early call**) and clarified **Domain**.

**The M2/M3 boundary, stated plainly:** M2's ~10-wave arc is a **showcase sequence** — each new
threat introduced once, brief combined pressure, a boss finale — proving the breadth vocabulary.
M3 then delivers **the** full _tuned_ campaign on the **two-path board** (the roadmap's R1 target
geometry), superseding this sequence. M2 deliberately does not attempt M3's pacing, difficulty
curve, or board.

## The cut line

| On at M2 (built now)                                                                                           | Off at M2 (activates later)                                                                                          |
| -------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| Ruleset as **schema-validated JSON** with a runtime load path (#22, ADR 0007 §2)                               | Two-path board + full tuned campaign (M3)                                                                            |
| **All six effect primitives** live: direct damage (single-target **and AoE**), slow, stun, DoT, support, burst | Board-geometry schema (multi-entrance, authored cell classes — M3, a later `formatVersion` + `simVersion` evolution) |
| **All five creep axes** live: speed, armor, domain (flying), immunities, boss role                             | Tower leveling & creep leveling; selectable targeting priority (M4)                                                  |
| **9-tower catalog** (8 showcases + 1 combo) · **8-creep catalog** (pairing set + 1 composed)                   | Easy / Hard difficulty tiers + the difficulty selector (tuned at M5)                                                 |
| **~10-wave showcase arc** with win/loss end to end                                                             | Economy tuning to final content; interest-on-savings decision (M5)                                                   |
| **Wave-clear + early-call bounty bonuses** at real values; **early-call score credit**                         | Efficiency scoring input (M5) · upgrade-value scoring input (M4)                                                     |
| **Wave preview** (next-wave composition on the countdown — PRD §2 amendment)                                   | Audio / sound                                                                                                        |
| Multi-tower **dock/picker UI**, per-tower info, top-dock header                                                | Best-score persistence (Phase 2) · leaderboard submission (Phase 3)                                                  |
| Full accessibility gate + externalized strings, per story                                                      | Mods / community rulesets (Phase 4)                                                                                  |

Difficulty runs on the single **Medium** reference curve (PRD §8; the ADR 0003 §3 waiver remains
recorded). Lives, win/loss, speed/pause, selling, and the board are **unchanged from M1**.

## Concrete spec

### Board

Unchanged from M1: the 28 × 24 single-path board (`m1.md` §Board), entrance col 0 row 11 → exit
col 27 row 11, centre-leak, same cell classes and maze invariant. Flying creeps traverse the
straight entrance→exit line above all of it (below).

### The towers

Nine catalog entries. **A tower is a data-defined bundle of effect primitives** (glossary: Effect
primitive); each **attacking** bundle carries a target-domain mask (`ground` / `air` / `both`);
a support-only bundle has **no target domain**. `basic` is M1's tower, unchanged.

| id             | Effects (authored order)                     | Domain | Cost | Damage | Range (tiles) | Cadence (ticks) | travel_ticks | Notes                                      |
| -------------- | -------------------------------------------- | ------ | ---- | ------ | ------------- | --------------- | ------------ | ------------------------------------------ |
| `basic`        | direct (single-target)                       | ground | 5    | 10     | 4             | 30              | 4            | M1 baseline, unchanged                     |
| `splash`       | direct (AoE, radius 1.5)                     | ground | 12   | 8      | 4             | 60              | 8            | long lob — dodgeable by design             |
| `slow`         | direct (single-target) + slow 50% / 40t      | both   | 8    | 2      | 4             | 30              | 2            | the both-domain tower; soft anti-air       |
| `stun`         | direct (single-target) + stun 25% / 20t      | ground | 10   | 4      | 3             | 40              | 2            | chance-based (sim RNG)                     |
| `venom`        | direct (single-target) + DoT 2 per 10t / 60t | ground | 9    | 2      | 4             | 30              | 2            | DoT bypasses armor                         |
| `beacon`       | support: adjacent damage ×1.5                | —      | 15   | —      | — (adjacency) | —               | —            | no attack; no target domain                |
| `mine`         | burst: direct (AoE, radius 1.5), one shot    | ground | 6    | 40     | 2 (trigger)   | — (single use)  | 1            | consumed at fire tick; lands next tick     |
| `antiair`      | direct (single-target)                       | air    | 7    | 8      | 5             | 20              | 2            | the dedicated air answer                   |
| `frost-splash` | direct (AoE, radius 1.5) + slow 30% / 30t    | ground | 16   | 6      | 4             | 60              | 8            | **the combo — pure data, zero sim change** |

Per-effect semantics (all first-pass; PRD 0001 §4 shapes):

- **AoE (direct damage's area form):** point-locked; the impact point leads the target along its
  current route at its fire-time effective speed, clamped to the exit; blast membership is the
  Euclidean point-distance test at the impact tick, radius in fixed-point tiles, **boundary
  inclusive** (`dist ≤ radius`, both in integer fixed-point — the pinned rounding).
- **Slow:** a fixed-point speed multiplier (`slow 50%` = ×128/256; `30%` = ×179/256), floor
  rounding, **floored at ⌈25% of the creep's base speed⌉**; duration refresh per the stacking rules
  (strongest magnitude wins; a weaker application neither extends nor overrides).
- **Stun:** halts movement for the duration; **overrides slow while active**; the 25% chance is one
  sim-RNG draw per application attempt (threshold 64/256), and an **immune target consumes no
  draw** (immunity filters before the roll).
- **DoT:** ticks every 10 ticks starting 10 ticks after application; **bypasses armor**; per-source
  independence (two `venom` towers = two DoTs); a re-application from the same source refreshes
  duration and adopts the new fire-time magnitude — **the tick cadence stays anchored to the
  first application and a refresh never resets or suppresses it**, so a DoT tick due on the
  refresh tick still applies (at the newly adopted magnitude, since impacts resolve before DoT
  ticks); **source identity = tower instance id** — a sold source's DoT keeps ticking but can no
  longer refresh.
- **Support:** recipients are **attacking** towers whose 2×2 footprint shares **≥ 1 full cell
  edge** with the beacon's (corner-only touch excluded; beacons do not buff beacons — no
  chaining). Buffed field: damage, ×384/256 (+50%), **floor-rounded to an integer** (an odd-damage tower buffs down — the same rounding rule as slow). **Overlapping auras: the strongest single aura
  wins — no stacking** (the same shape as the status stacking rule). Buffs enter a shot via the
  fire-time snapshot; adjacency recomputes deterministically on every build/sell.
- **Burst:** arms on placement; fires its one AoE discharge at the first tick a target is inside
  its **trigger range** (2 tiles), and the tower is **consumed at that fire tick**. Routing is
  per-tick derived state: the fire tick's own movement step **precedes** the consumption (combat
  runs after movement) and correctly sees the mine still standing; **every movement step after
  the consumption — beginning with the very next movement phase — uses the post-consumption
  maze** (PRD §3's re-route with no stale step thereafter); the pinned tick-level contract is **one routing snapshot per
  tick**, derived before movement — movement, targeting, and AoE lead all read it, and a
  mid-combat-phase consumption never re-derives mid-phase (towers firing later that combat phase
  read pre-consumption distances). The discharge itself resolves from its fire-time snapshot. A
  pre-fire `mine` sells for the normal 75% refund; a fired one is gone (no refund).

### The creeps

Eight catalog entries. **Creeps vary along the five axes** (speed, durability = HP + armor,
domain, effect-immunities, role); every entry carries a bounded **`leakCost`** (how many lives a
leak costs). `normal` is M1's creep, unchanged.

| id              | HP  | Speed (fp/tick) | Armor | Domain | Immunities | Role | leakCost | Kill bounty | Answers / answered by                         |
| --------------- | --- | --------------- | ----- | ------ | ---------- | ---- | -------- | ----------- | --------------------------------------------- |
| `normal`        | 20  | 26              | 0     | ground | —          | —    | 1        | 1           | M1 baseline                                   |
| `swarm`         | 7   | 30              | 0     | ground | —          | —    | 1        | 1           | dies to one `splash` blast — AoE's showcase   |
| `fast`          | 16  | 44              | 0     | ground | —          | —    | 1        | 2           | outruns fixed fire — `slow`'s showcase        |
| `armored`       | 36  | 22              | 6     | ground | —          | —    | 1        | 3           | blanks small hits — `venom`'s showcase        |
| `flying`        | 18  | 30              | 0     | air    | —          | —    | 1        | 2           | ignores the maze — `antiair`'s showcase       |
| `resolute`      | 20  | 44              | 0     | ground | slow       | —    | 1        | 2           | punishes slow-reliance — immunity's showcase  |
| `armored-flyer` | 30  | 26              | 5     | air    | —          | —    | 1        | 3           | **the composed creep** — axes compose as data |
| `boss`          | 400 | 18              | 8     | ground | stun       | boss | **3**    | 25          | the finale; cannot be stun-locked             |

Axis semantics (first-pass; PRD 0001 §5 shapes):

- **Armor** is a flat per-hit reduction on **direct** hits only, floored at zero — armor ≥ damage
  fully negates the hit (`splash`'s 8 vs the boss's 8 → 0, deliberately: the boss demands DoT,
  support-buffed damage, or `mine`). DoT bypasses armor entirely.
- **Immunity flags** are boolean; an immune target neither receives the effect nor (for
  chance-based effects) consumes an RNG draw.
- **Flying** creeps travel the straight entrance→exit line, over towers, walls, and blocked cells;
  they are hit only by `air`/`both` towers, they **do not block placement** (the
  never-build-on-a-creep rule tests ground creeps only), and they never constrain the maze
  invariant. Their "route" for targeting distance and AoE lead is that straight line.
- **Boss** (glossary): role, not a kind of stat — high durability, wave centerpiece, and a leak
  costs `leakCost` lives (**3**), which may overshoot lives below zero (PRD §7).
- Every spawned creep carries its **wave identity** (which wave spawned it) — the per-wave clear
  bonus depends on it.

### Combat resolution order & status semantics (the pinned total order)

The tick's outer frame is the phase order as built: **input → wave → field-derivation → movement →
combat → resolution**. Within the combat phase, the cross-effect order is pinned (this is the
determinism-gated detail PRD §4 delegates to the combat build):

1. Impacts due this tick resolve in **queue insertion order** — itself deterministic because
   towers fire in tower-id order (the scheduling key).
2. Within one impact, the nesting is: **outer loop = affected creeps in creep-id order** (an AoE's
   radius set; a single target trivially); per creep, **direct-damage effects apply in the
   bundle's authored relative order → death check → surviving creeps receive status effects in the
   bundle's authored relative order**. A lethal hit applies no statuses. Loaders normalize bundles
   so this traversal is well-defined.
3. After all due impacts, **DoT ticks** resolve over creeps in creep-id order, per-source in
   application order.
4. One **dead-creep sweep** then removes every creep killed this phase and credits its kill
   bounty — impacts and DoTs mark deaths, a single deterministic compaction collects them.
5. **Target acquisition and tower firing** run over the post-sweep world in tower-id order; a
   tower whose held target died earlier this same phase may reacquire and fire this tick.
6. **RNG draws occur only inside that traversal, in exactly its order** (creep-then-effect).
7. Status **durations end inclusively** (an effect applied at tick T for duration D is active
   through tick T + D). A target-locked impact on a dead/leaked creep is wasted; an AoE resolves
   against whoever is alive in radius at the impact tick.

### Waves & timing

**Multi-wave state model** (replaces M1's single `pre-wave | active`): the **run phase** (held /
running / terminal) separates from a **per-wave lifecycle** — each wave tracks
`scheduled → counting-down → spawning → resolved`. Countdowns are **sequential** (PRD §2): **at most
one** wave is counting down at a time — exactly one while any scheduled wave remains, none
once the final wave has launched; launching it (by expiry or early call) starts the next wave's
countdown **on the following tick**, so countdowns run **while prior waves' creeps are still on
the field**, and overlapping waves are the norm. A wave is **resolved** when its spawn schedule is
exhausted and none of its creeps remain alive.

**The arc — 10 waves** (composition · spacing · countdown before launch · clear bonus):

| #   | Composition                                              | Spacing (ticks)  | Countdown (ticks) | Clear bonus | Teaches                          |
| --- | -------------------------------------------------------- | ---------------- | ----------------- | ----------- | -------------------------------- |
| 1   | 10 × `normal`                                            | 20               | 500               | 4           | M1's wave, verbatim — continuity |
| 2   | 16 × `swarm`                                             | 5                | 300               | 4           | AoE                              |
| 3   | 8 × `fast`                                               | 15               | 300               | 5           | slow                             |
| 4   | 6 × `armored`                                            | 25               | 300               | 5           | armor / DoT                      |
| 5   | 12 × `normal` + 6 × `swarm`                              | 12 / 5           | 300               | 6           | combined pressure                |
| 6   | 8 × `flying`                                             | 15               | 300               | 6           | air / anti-air (preview warns)   |
| 7   | 6 × `resolute` + 6 × `fast`                              | 15 / 15          | 300               | 7           | immunity                         |
| 8   | 6 × `armored-flyer`                                      | 20               | 300               | 7           | composed axes                    |
| 9   | 10 × `swarm` + 6 × `fast` + 4 × `armored` + 4 × `flying` | 6 / 12 / 20 / 20 | 300               | 8           | combined, all-domain             |
| 10  | 1 × `boss` + 8 × `normal`                                | — / 20           | 400               | 12          | the finale                       |

Mixed waves spawn their entries as **concurrent streams**: by default every stream's first
spawn is on the launch tick (offset 0), each stream continues on its own spacing, and
simultaneous spawns order deterministically by table-row order (wave 10: the boss on the launch
tick, escorts every 20 ticks from it). Per-stream offsets are schedule data that may override
the default. When spawns from **different overlapping waves** fall due on the same tick, the
**earlier-launched wave spawns first**, then table-row order within each wave — pinned because
spawn order assigns creep ids, which drive every id-based tie-break downstream.

**Wave preview** (glossary; PRD §2 as amended): the countdown UI shows the **next wave's
composition, derived from the ruleset wave schedule** (authoritative — never duplicated UI data):
aggregated counts per creep kind in schedule order, each entry exposing **name (i18n key), count,
domain, armor, and immunities** as accessible text (never color/icon-only — ADR 0003). One wave of
lookahead only. After the final wave launches, the preview shows an explicit last-wave state.

**Starting ≠ claiming (semantic pin, not a UI topology):** beginning the run banks nothing — the
first countdown simply starts ticking (it is callable from tick 0, PRD §2). The **early-call
reward is claimed only by a deliberate player act**. The recommended affordance is **one primary
control that morphs** ("Start" while held → "Call wave" for the rest of the run); the exact
affordance is the story's design call under ADR 0003 and the two-layouts contract. Today's Start
control enqueues `callWaveEarly` directly — that coupling must be gone **before** any nonzero
bonus or credit activates.

### Economy

- **Income sources now all live** (PRD §6): per-kill bounty (per the creep table), the per-wave
  **clear bonus** (table above), and the **early-call bounty bonus**.
- **Per-wave clear bonus:** paid **once**, at the wave's resolution tick, **forfeited if any of
  that wave's creeps leaked** (per-wave leak flag over the creep's wave identity — one wave's leak
  never forfeits another's bonus).
- **Early-call bounty bonus:** `⌊ticksRemaining / 50⌋` bounty (≈ 1 per 2.5 s shaved; up to 10 on
  wave 1, 6 on 300-tick countdowns). **Auto-launch pays 0.**
- **Starting bounty 80**, sell refund `⌊75% × cumulative spend⌋` — both unchanged from M1.
  Selling a `beacon` recomputes auras that tick; a fired `mine` cannot be sold (it no longer
  exists).

### Scoring & star grade

- **Numeric score** (outcome-dependent — product ruling, Rob 2026-07-26): on a **win**,
  `Σ kill-bounties + Σ early-call score credits + (max(0, lives_remaining) × 35)`; on a **loss**,
  `Σ kill-bounties` only — **early-call credits are forfeited at a loss terminal** (the tempo
  bet didn't pay off) and the survival term keeps the authoritative M1 scorer's **zero floor**
  (a multi-life boss overshoot never subtracts score). **Win-over-loss ordering is a pinned
  ruleset balance invariant:** the best achievable losing score must stay below the worst
  achievable winning score, verified by S11's balance pass and re-verified whenever numbers
  are retuned. With first-pass numbers: best loss = 201 (kill everything, leak ten 1-bounty
  creeps); worst win = 203 (leak the boss plus six 3-bounty creeps, finish on 1 life:
  168 + 35). The margin is deliberately thin at first-pass — S11 owns widening it.
  The **early-call score credit** (glossary: Early call) comes online per PRD §7:
  **`⌊ticksRemaining / 50⌋` points per deliberate early call** (≤ 10 on wave 1, ≤ 6 on 300-tick
  countdowns, ≤ 8 on the finale; ≈ 66 across a maximally aggressive run — meaningful beside the
  211 total kill bounty of the arc, roughly ⅓, not dominant). **Auto-launch earns 0.** Credit accrues in a dedicated cumulative counter beside
  `cumulativeKillBounty`; all additions are **guarded (saturating) within the safe-integer
  domain** (operands bounded: ticksRemaining ≤ 500).
- **Live HUD score accrues earned-so-far inputs** — kill bounty **and** early-call credit, the
  moment each is earned (product ruling, Rob 2026-07-26). The survival term remains
  **terminal-only**, and on a **loss** the authoritative terminal score excludes the forfeited
  credits — it can land **below** the last live readout. Deliberate, and consistent with the
  #53 ruling: the live score is earned-so-far, and no monotonicity is promised.
- **Star grade unchanged from M1** (3★ ≥ 9 lives · 2★ ≥ 6 · 1★ ≥ 1; loss = none) — thresholds
  re-tuned only if playtests demand (Open questions).

### Speed & pause

Unchanged from M1: pause (build-while-paused) / 1× / 2×; presentation-layer, outside the ruleset
and the world-hash (ADR 0006).

### Art (sprite additions)

Eight new tower sprites (distinct silhouettes; `frost-splash` reads as its two parents), seven new
creep sprites (each silhouette distinct at cell scale; `armored-flyer` reads as armored + airborne),
**status telegraphs** (slowed / stunned / DoT / immune-flash — dual-encoded shape + motion cues,
never color alone), an aura ring for `beacon` adjacency, a blast ring for AoE, and **wave-preview
icons** (paired with text, never icon-only). All original and license-clean per ADR 0002.

### Accessibility & i18n

- The full ADR 0003 gate applies to **every player-facing story** — each completes the checklist
  or records a waiver in its PR; the closing UI story carries only the milestone-level real-device
  checks. The Medium-only difficulty waiver remains recorded (M5 lifts it).
- Every new string externalized per ADR 0004: tower/creep names and descriptions, preview text,
  status announcements, picker labels.

## First-pass numbers (the tuning surface)

The tower, creep, and wave tables above **are** the tuning surface — all ruleset data (ADR 0007);
a change bumps `rulesetHash`. Global knobs:

| Knob                    | M2 first-pass value                                           |
| ----------------------- | ------------------------------------------------------------- |
| Board / lives / refund  | unchanged from M1 (28 × 24 · 10 lives · 75% refund)           |
| Starting bounty         | 80 (unchanged)                                                |
| Slow floor              | ⌈25% × base speed⌉                                            |
| Stun chance / duration  | 64/256 (25%) / 20 ticks (`stun` tower)                        |
| DoT cadence             | every 10 ticks, first tick at +10                             |
| Support buff            | damage ×384/256 (+50%), strongest single aura                 |
| Early-call bounty       | ⌊ticksRemaining / 50⌋ (auto-launch 0)                         |
| Early-call score credit | ⌊ticksRemaining / 50⌋ (auto-launch 0)                         |
| Score                   | win: Σ kills + Σ credits + max(0, lives) × 35 · loss: Σ kills |
| Star thresholds         | 3★ ≥ 9 · 2★ ≥ 6 · 1★ ≥ 1 (unchanged)                          |

## Stories (the build plan)

Twelve stories, each ≈ one PR with its own done-criteria, each delivering playable value. **Every
content story wires its evolving bundle into the shipped default** (client and server load the
updated artifact through S1's registry) — that is what makes each slice playable. `simVersion`
bumps at S2–S10 (nine intra-M2 bumps: alpha-line replays are valid within a build, not across).

1. **Ruleset as data (#22).** The M1 ruleset moves to **schema-validated JSON** served to **both
   independent loaders — client and server** (ADR 0007 §2; encoding per
   `design-notes/ruleset-format.md`): the server's hard-wired `m1Ruleset` import and the client's
   stamped board id become a small bundle registry/load path; parse-failure behavior defined;
   malformed JSON rejected. S1 pins the **complete v2 schema structurally** (string catalog ids
   `[a-z0-9-]+` ≤ 64 chars; discriminated effect-bundle arrays covering all six effect kinds;
   all five axis fields including bounded per-creep `leakCost`, replacing the global
   `balance.leakCost` authority) — `formatVersion` is then **fixed for all of M2**. A shared
   per-`simVersion` **capability profile** gates semantics: kinds, cardinalities, and values
   (waves ≤ 1 until S2; effect/axis kinds rejected until their story; `leakCost` = 1 until S10).
   **Digest parity with the TS-era bundle is dropped** (the normalized shape changes; nothing
   consumes old hashes pre-persistence — supersedes #22's digest-unchanged note); S1 instead
   proves **behavioral parity**: the v2-loaded M1 bundle simulates **world-hash-identical** over
   a golden replay.
   _Done:_ client and server load + validate the same JSON artifact; behavioral-parity golden
   passes; malformed/capability-violating bundles rejected with tests.
2. **The run gets long.** The multi-wave state model (above); the wave schedule grows to N waves
   (M1 content only — 3 × wave-1 clones is fine here); per-wave clear tracking via creep wave
   identity; wave-clear + early-call bounty bonuses at real values; the early-call score credit
   accruing live (forfeited at a loss terminal); **wave preview** per the pinned model; **starting ≠ claiming** (the Start
   coupling to `callWaveEarly` removed); **the multi-wave terminal-tick bound re-derived and
   hostile-replay ceilings updated** (the one-wave compiler bound is structurally invalid the
   moment this story lands). `simVersion` bump.
   _Done:_ a 3-wave run resolves win/loss with per-wave bonuses paid/forfeited correctly; preview
   shows next-wave composition; starting banks nothing; bounds tests updated; world-hash stable.
3. **Slow + fast creep + the picker.** The status-effect framework per the pinned order (§Combat);
   the `placeTower` command becomes `{ kind: 'placeTower', anchor, towerId }` — **catalog-unknown
   or malformed `towerId` is structurally malformed input and invalidates the replay (ADR 0006
   §4); only a well-formed, catalog-known id with an illegal placement is the deterministic
   no-op**. Tower state stores kind; a **minimal accessible picker** (full ADR 0003 checklist);
   per-creep max HP enters the render VM (fixing the documented single-kind HP-fraction
   assumption); the `slow` tower (ground-scoped until S7 activates domains) and `fast` creep;
   slowed telegraph. `simVersion` bump (gates the command change).
   _Done:_ two towers placeable via picker and keyboard; slow visibly and deterministically slows
   (floor respected); replay with unknown towerId rejected; a11y checklist recorded.
4. **AoE + swarm.** Direct damage's area form per §The towers (lead, clamp, inclusive boundary,
   creep-id blast order); the `splash` tower and `swarm` creep; blast presentation. **The ADR 0005
   seeded stress scenario is built here and its measured spike run and recorded** (real low-end
   Android + Chrome emulation, percentile reporting per the ADR), then extended and re-measured by
   every subsequent effect story; AoE scan-work caps join the compiler constraints. `simVersion`
   bump.
   _Done:_ blasts resolve deterministically at scale; spike recorded; swarm wave clears with one
   well-placed `splash`.
5. **DoT + armored creep.** The `venom` tower and `armored` creep; armor math (flat, floored,
   full negation) and DoT bypass per §The creeps; per-source DoT state caps join the compiler
   constraints; armor/immunity/domain surfaced in wave-preview entries (the pre-wave inspection
   surface). `simVersion` bump.
   _Done:_ armored creeps blank `basic` to 4/hit and die to DoT on schedule; preview exposes
   armor; caps tested.
6. **Stun + immune counterplay.** The `stun` tower and `resolute` creep; chance-based stun per
   the pinned RNG rule (immune targets consume no draw); stun-overrides-slow; immunity feedback in
   telegraphs and preview. `simVersion` bump.
   _Done:_ stun chains are deterministic per seed; `resolute` shrugs slow with visible feedback;
   RNG-draw order tested.
7. **Air.** Domain masks on towers and creeps per §The towers/§The creeps; straight-line flight;
   the **both-domain targeting metric in the engine's own units** — ground = the existing scaled
   octile route distance (`ORTHO_COST`/`DIAG_COST` × `DIST_SCALE`), air =
   `⌊airDistanceFp × ORTHO_COST × DIST_SCALE / FP_ONE⌋` (floor-rounded integer sqrt), compared in
   that shared unit, ties to the lower creep id; the **target-domain mask joins the fire-time
   impact snapshot** (a sold/consumed source no longer exists at impact — the snapshot is the
   authority); flyers don't block placement or the maze invariant (tests); the `antiair` tower
   (and `slow` goes both-domain); the `flying` creep. `simVersion` bump.
   _Done:_ flyers cross a full maze untouched by ground towers, die to `antiair`, are slowed by
   `slow`; placement-over-flyer works; metric comparison unit-tested at boundaries.
8. **Support.** The `beacon` per §The towers (adjacency, recipients, strongest-single-aura,
   fire-time snapshot, deterministic recompute on build/sell); aura presentation. `simVersion`
   bump.
   _Done:_ buffed towers hit for exactly ×1.5 snapshot damage; aura changes on build/sell are
   deterministic; no beacon-chaining.
9. **Burst.** The `mine` per §The towers (trigger, one discharge, fire-tick consumption, the
   routing-snapshot contract, sell/refund interplay); consumption presentation. `simVersion` bump.
   _Done:_ a triggered mine deletes itself, creeps re-route from the next movement step with no
   stale-maze step, its discharge still lands; pre-fire sell refunds 75%.
10. **The finale content.** The `boss` (stun-immune; `leakCost` 3 activates multi-life leak with
    below-zero overshoot — this story's one sim rule) and `armored-flyer`; the `frost-splash`
    combo tower — **pure data against the fixed v2 schema, zero sim change, which is the
    composability proof**. `simVersion` bump (boss leak rule).
    _Done:_ a boss leak costs 3 lives (overshoot tested); `frost-splash` ships with no sim diff;
    all 9 towers and 8 creeps in the default bundle.
11. **The showcase run.** The full 10-wave arc authored per the table; the whole-run balance pass
    across the final catalog; the **final catalog-scale ADR 0005 stress gate**; the **M2
    determinism golden replay**; the end-to-end **client/server replay-parity test** over the
    finished bundle. Content + verification only — no sim change.
    _Done:_ the arc is winnable and loseable in real play; golden + parity + stress all green.
12. **UI breadth.** Dock UX at 9 towers (picker ergonomics/scroll on both layouts under the
    two-layouts contract — Compact ≤ 500 px is the flagged design corner); per-tower info at build
    time (cost/damage/range/effects/domain); the **top-dock header** (a measured mitigation of the
    iOS Control Center edge-swipe conflict, validated on the named playtest devices); **#36
    close-out with the accessibility checklist's required real-device evidence recorded** (if any
    required device check is missing, #36 stays open and this story says what remains). Render/
    input only — no sim change.
    _Done:_ 9-tower dock usable on the smallest supported phone; header mitigation measured; #36
    resolved or explicitly re-scoped.

## Determinism & data

- **Layering unchanged:** render/input read sim state, never mutate; all gameplay stays a pure
  function of `(seed, ruleset, boardId, tickInputs)` **under the replay's stamped `simVersion`**
  — the version selects the implementation, and the validator accepts only its own (ADR 0006).
- **simVersion cadence:** nine bumps (S2–S10) — each sim-shape change is one story's bump;
  alpha-line replays are valid within a build, never across. The `placeTower` change is gated by
  `simVersion` (ADR 0006 defines no separate replay version and M2 introduces none).
- **Schema policy:** the complete v2 structure is fixed at S1 (`formatVersion` stable through M2);
  the per-`simVersion` **capability profile** is the only thing that grows per story. Content
  changes bump `rulesetHash` per story (expected). M3's board geometry is an acknowledged later
  `formatVersion` + `simVersion` evolution — deliberately **not** pre-built into v2.
- **ADR 0006 §2's stale parenthetical** ("when the previous wave clears") is corrected by a dated
  note in this PR: wave timing chains off the previous **launch** (PRD §2); the ADR's decision —
  spawns from the ruleset schedule inside the deterministic sim — stands.
- **Bounds discipline:** every story that invalidates a bound re-derives it in the same story
  (multi-wave bounds in S2; AoE scan caps in S4; DoT state caps in S5; resident-impact budgets as
  cadence/travel profiles change). The ADR 0005 spike is measured at S4 and re-measured as
  behaviors land; S11 runs the catalog-scale gate.

## Open questions / first-pass flags

Edges for reviewers — deliberate judgment calls, all tunable:

- **The credit weights** (`/50` for both bounty and score, coinciding at first-pass) set
  maximally aggressive early-calling at ≈ ⅓ of the arc's
  kill score when played maximally aggressively. If playtests show call-spam dominating score,
  tune the divisor before touching the mechanic.
- **`splash` fully negated by boss armor (8 vs 8)** is deliberate (the boss demands DoT/support/
  mine answers), but it is the spec's sharpest balance edge — revisit if the finale feels unfair.
- **S2 is the widest story**; its natural split seam is the preview UI if review pressure demands.
- **Mixed-wave stream offsets** carry a pinned default (all streams start at launch, offset 0);
  per-stream overrides are schedule data — tune pressure there, not in code.
- **Star thresholds unchanged from M1** — a 10-wave run may deserve its own curve; tune against
  real runs at S11.
- **Dock UX on Compact** (≤ 500 px height) with 9 towers is the hardest UI corner; designed
  in-story under the two-layouts contract (no new breakpoint).
